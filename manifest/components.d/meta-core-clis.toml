[[component]]
id = "git"
name = "Git SCM (meta-hosted binary tree)"
description = "Git 2.54.0 copied into $META_ROOT/.toolchains/git as a real binary tree. No wrapper: $META_ROOT/usr/bin/git is a direct symlink to the meta-hosted git executable."
[component.detect]
kind = "command"
command = "bash"
args = ["-lc", "M=\"${META_ROOT:?META_ROOT required}\"; [ -x \"$M/.toolchains/git/bin/git\" ] && [ \"$(readlink -f \"$META_ROOT/usr/bin/git\" 2>/dev/null)\" = \"$(readlink -f \"$M/.toolchains/git/bin/git\")\" ]"]
[component.install]
kind = "script"
script = '''
set -euo pipefail
M="${META_ROOT:?META_ROOT required}"
DEST="$M/.toolchains/git"
install -d -m 755 "$META_ROOT/usr/bin"
PATH="$(printf '%s' "$PATH" | tr ':' '
' | grep -v -E "^$META_ROOT/usr/bin$" | paste -sd: -)"
src_bin="$(readlink -f "$(command -v git)")"
case "$src_bin" in "$DEST"/*) src_bin="$(find /nix/store -path '*/bin/git' -type f 2>/dev/null | grep 'git-2.54.0' | head -n1)";; esac
[ -n "$src_bin" ] || { echo "FATAL: no Git 2.54.0 source binary found"; exit 1; }
src_root="$(cd "$(dirname "$src_bin")/.." && pwd)"
rm -rf "$DEST"
mkdir -p "$DEST"
cp -a "$src_root/." "$DEST/"
ln -sfn "$DEST/bin/git" "$META_ROOT/usr/bin/git"
'''
[component.verify]
kind = "command"
command = "bash"
args = ["-lc", "M=\"${META_ROOT:?META_ROOT required}\"; export PATH=\"$META_ROOT/usr/bin:$PATH\"; [ \"$(readlink -f \"$(command -v git)\" 2>/dev/null)\" = \"$(readlink -f \"$M/.toolchains/git/bin/git\")\" ] && git --version | grep -q '2\\.54\\.0'"]
[component.fix]
kind = "script"
script = '''
set -euo pipefail
M="${META_ROOT:?META_ROOT required}"
DEST="$M/.toolchains/git"
install -d -m 755 "$META_ROOT/usr/bin"
PATH="$(printf '%s' "$PATH" | tr ':' '
' | grep -v -E "^$META_ROOT/usr/bin$" | paste -sd: -)"
src_bin="$(readlink -f "$(command -v git)")"
case "$src_bin" in "$DEST"/*) src_bin="$(find /nix/store -path '*/bin/git' -type f 2>/dev/null | grep 'git-2.54.0' | head -n1)";; esac
[ -n "$src_bin" ] || { echo "FATAL: no Git 2.54.0 source binary found"; exit 1; }
src_root="$(cd "$(dirname "$src_bin")/.." && pwd)"
rm -rf "$DEST"
mkdir -p "$DEST"
cp -a "$src_root/." "$DEST/"
ln -sfn "$DEST/bin/git" "$META_ROOT/usr/bin/git"
'''
[component.remove]
kind = "command"
command = "bash"
args = ["-lc", "M=\"${META_ROOT:?META_ROOT required}\"; t=\"$META_ROOT/usr/bin/git\"; [ -L \"$t\" ] && readlink \"$t\" | grep -q \"$M/.toolchains/git\" && rm -f \"$t\"; rm -rf \"$M/.toolchains/git\""]
[component.wiring]
path_entries = ["$META_ROOT/usr/bin"]

[[component]]
id = "curl"
name = "curl (static meta binary)"
description = "Static curl binary installed under $META_ROOT/.toolchains/curl. No wrapper: $META_ROOT/usr/bin/curl is a direct symlink to the meta-hosted curl binary."
[component.detect]
kind = "command"
command = "bash"
args = ["-lc", "M=\"${META_ROOT:?META_ROOT required}\"; [ -x \"$M/.toolchains/curl/bin/curl\" ] && [ \"$(readlink -f \"$META_ROOT/usr/bin/curl\" 2>/dev/null)\" = \"$(readlink -f \"$M/.toolchains/curl/bin/curl\")\" ]"]
[component.install]
kind = "script"
script = '''
set -euo pipefail
M="${META_ROOT:?META_ROOT required}"
VER="${CURL_VERSION:-8.21.0}"
DEST="$M/.toolchains/curl"
ARCH="$(uname -m)"; case "$ARCH" in x86_64) A=x86_64;; aarch64) A=aarch64;; *) echo "unsupported arch: $ARCH"; exit 1;; esac
URL="https://github.com/stunnel/static-curl/releases/download/$VER/curl-linux-$A-musl-$VER.tar.xz"
cache="$M/.cache/curl"; install -d -m 755 "$cache" "$DEST/bin" "$META_ROOT/usr/bin"
tarball="$cache/curl-linux-$A-musl-$VER.tar.xz"
[ -s "$tarball" ] || /usr/bin/curl -fSL "$URL" -o "$tarball"
tmp="$(mktemp -d)"; trap 'rm -rf "$tmp"' EXIT
tar -xJf "$tarball" -C "$tmp"
src="$(find "$tmp" -type f -name curl -perm -111 | head -n1)"
install -m 755 "$src" "$DEST/bin/curl"
ln -sfn "$DEST/bin/curl" "$META_ROOT/usr/bin/curl"
'''
[component.verify]
kind = "command"
command = "bash"
args = ["-lc", "M=\"${META_ROOT:?META_ROOT required}\"; export PATH=\"$META_ROOT/usr/bin:$PATH\"; [ \"$(readlink -f \"$(command -v curl)\" 2>/dev/null)\" = \"$(readlink -f \"$M/.toolchains/curl/bin/curl\")\" ] && curl --version | grep -q '8\\.21\\.0'"]
[component.fix]
kind = "script"
script = '''
set -euo pipefail
M="${META_ROOT:?META_ROOT required}"
VER="${CURL_VERSION:-8.21.0}"
DEST="$M/.toolchains/curl"
ARCH="$(uname -m)"; case "$ARCH" in x86_64) A=x86_64;; aarch64) A=aarch64;; *) echo "unsupported arch: $ARCH"; exit 1;; esac
URL="https://github.com/stunnel/static-curl/releases/download/$VER/curl-linux-$A-musl-$VER.tar.xz"
cache="$M/.cache/curl"; install -d -m 755 "$cache" "$DEST/bin" "$META_ROOT/usr/bin"
tarball="$cache/curl-linux-$A-musl-$VER.tar.xz"
[ -s "$tarball" ] || /usr/bin/curl -fSL "$URL" -o "$tarball"
tmp="$(mktemp -d)"; trap 'rm -rf "$tmp"' EXIT
tar -xJf "$tarball" -C "$tmp"
src="$(find "$tmp" -type f -name curl -perm -111 | head -n1)"
install -m 755 "$src" "$DEST/bin/curl"
ln -sfn "$DEST/bin/curl" "$META_ROOT/usr/bin/curl"
'''
[component.remove]
kind = "command"
command = "bash"
args = ["-lc", "M=\"${META_ROOT:?META_ROOT required}\"; t=\"$META_ROOT/usr/bin/curl\"; [ -L \"$t\" ] && readlink \"$t\" | grep -q \"$M/.toolchains/curl\" && rm -f \"$t\"; rm -rf \"$M/.toolchains/curl\""]
[component.wiring]
path_entries = ["$META_ROOT/usr/bin"]
