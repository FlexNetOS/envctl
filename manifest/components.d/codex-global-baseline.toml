[[component]]
id = "codex-global-baseline"
name = "Codex active-home policy and runtime-shadow retirement"
description = "Preserves unrelated editable active-home Codex settings, projects the tracked RTK/Codex instruction surfaces, permits only canonical remote URL MCP entries, disables plugin discovery through the profile-owned official CLI, rejects plugin/marketplace authority, delegates binary ownership to Yazelix, and archives forbidden generated runtime shadows. RTK arrives only through the same Yazelix foundation that owns Codex."
requires = ["codex-cli"]

# This component deliberately does not regenerate config, models, agents,
# plugins, marketplaces, or a second state root. It preserves one canonical
# Codex lifecycle dispatcher through the repository-owned lifecycle script.
[component.detect]
kind = "shipped_script"
path = "$ENVCTL_SOURCE_ROOT/assets/scripts/envctl-codex-global-baseline-lifecycle.sh"
args = ["detect"]

[component.install]
kind = "shipped_script"
path = "$ENVCTL_SOURCE_ROOT/assets/scripts/envctl-codex-global-baseline-lifecycle.sh"
args = ["install"]

[component.verify]
kind = "shipped_script"
path = "$ENVCTL_SOURCE_ROOT/assets/scripts/envctl-codex-global-baseline-lifecycle.sh"
args = ["verify"]

[component.fix]
kind = "shipped_script"
path = "$ENVCTL_SOURCE_ROOT/assets/scripts/envctl-codex-global-baseline-lifecycle.sh"
args = ["fix"]

[component.remove]
kind = "shipped_script"
path = "$ENVCTL_SOURCE_ROOT/assets/scripts/envctl-codex-global-baseline-lifecycle.sh"
args = ["remove"]
