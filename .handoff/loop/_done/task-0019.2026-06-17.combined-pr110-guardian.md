# Verification report: COMBINED branch `task-0019-frontend-gaps` (PR #110)

5 commits: (1) full kasetto v3.2.0 CLI/GUI option parity (TASK-0019) · (2) agent doctor
config-less no-downgrade fix · (3) kasetto short-forms `rm`/`-u`/`-P` · (4) env-mutating-test
serialization (init_path_global CI race) · (5) `low-cost-kdf-tests` CI-speed feature.

## Verdict — **PASS**

Every CI gate (incl. the NEW `kdf-feature-off`), `cargo fmt`/`clippy`/both `test` invocations,
and the GUI build are green. All NON-NEGOTIABLE invariants hold against the delivered code. The
KDF test-speed feature is provably production-intact: floor/Default constants byte-for-byte
unchanged on default features, pinned by a default-only test, off-by-default (gate-proven), and
unshippable (release+feature trips `compile_error!`, verified exit 101). No blocking findings.

## Gate results (exit codes pasted)
| Gate | exit | Evidence |
|------|------|----------|
| `ci/gates/no-c.sh` | **0** | `resolved graph clean: rustls=['0.23.40'] on ring=['0.17.14']; zero aws-lc/openssl/C-SQLite` — KDF feature added NO C dep |
| `ci/gates/shape.sh` | **0** | `SHAPE GATE PASS` |
| `ci/gates/enable.sh` | **0** | `ENABLE GATE PASS` |
| `ci/gates/p7.sh` | **0** | `P7 GATE PASS` |
| `ci/gates/kdf-feature-off.sh` (NEW) | **0** | `default features for envctl-secrets-engine = ['inmem-store', 'mitm-ca'] (low-cost-kdf-tests correctly OFF by default)` → `KDF-FEATURE-OFF GATE PASS` |

## cargo
| Check | exit | Evidence |
|-------|------|----------|
| `cargo fmt --all --check` | **0** | clean |
| `cargo clippy --workspace -- -D warnings` | **0** | full workspace incl. GUI/secretd/secretctl, `Finished` 0 warnings |
| `cargo test --workspace` (DEFAULT / full-cost crypto) | **0** | 36 suites ok · **757 passed; 0 failed** · `keyslot::tests::production_floor_constants_pinned ... ok` · wall **7:25.81** |
| `cargo test --workspace --features envctl-secrets-engine/low-cost-kdf-tests` (CI invocation) | **0** | 36 suites ok · **756 passed; 0 failed** · wall **0:08.98** (~50× faster) |
| `cargo build -p envctl-gui` | **0** | `Finished dev profile`, no system-lib block |

Test-count delta default(757)→feature(756) = exactly the one `#[cfg(not(feature = …))]`-gated
`production_floor_constants_pinned` test (correctly compiled OUT under the feature). 0 failed both.

## KDF feature — production-crypto-intact findings (no-downgrade-sensitive)
- **Constants pinned under `#[cfg(not(feature = "low-cost-kdf-tests"))]`** (keyslot.rs):
  `ARGON2_M_KIB_FLOOR = 262_144` (L106), `ARGON2_T_COST_FLOOR = 3` (L118), `Default = { m_kib:
  1_048_576, t_cost: 4, p_lanes: 4 }` (1 GiB / t4 / 4 lanes). Feature branch lowers to 8 / 1 /
  {8,1,1} — confined entirely behind `#[cfg(feature = …)]`.
- **Downgrade-guard UNCHANGED & still compares against the named constants** (keyslot.rs:292-301):
  `p.m_kib >= ARGON2_M_KIB_FLOOR` and `p.t_cost >= ARGON2_T_COST_FLOOR` — guard logic untouched;
  it tracks whichever floor is in effect (correct/consistent, not a downgrade of the guard itself).
- **`production_floor_constants_pinned`** (default-only test) asserts 262144 / 3 / (1 GiB,t4,p4) and
  RAN GREEN on the default-features run (test_default.txt:767). Absent (cfg-gated) on the feature run.
- **`compile_error!` tripwire UNSHIPPABLE**: `cargo build --release -p envctl-secrets-engine
  --features low-cost-kdf-tests` → **exit 101** with `low-cost-kdf-tests lowers the Argon2 floor —
  CI test-speed only (TASK-0032); never in a release build`. (cfg = `all(feature, not(debug_assertions))`
  so dev/test builds compile, release builds fail — the correct seam.)
- **`.github/workflows/ci.yml`**: ONLY the `test` step enables the feature (L77). `rustfmt` (L34),
  `clippy` (L49), and all `gates` (L88-97, incl. the new kdf-feature-off gate) use default features.
- **no-c**: the feature is `low-cost-kdf-tests = []` (zero deps, cfg-only) — no crate added; no-c PASS
  confirms one ring-only rustls and zero C, identical to the pre-feature graph.

## Invariant checks
1. **No C in trust boundary** — PASS. no-c.sh: one `rustls 0.23.40` on `ring 0.17.14`, zero
   aws-lc/openssl/C-SQLite. KDF feature = empty (no dep). clap_complete (TASK-0019) pure-Rust.
2. **Exactly one rustls, ring-only** — PASS (no-c output).
3. **Engine non-printing / sync / pure-Rust** — PASS. Grep of `self_update.rs`,
   `self_uninstall.rs`, `update_notifier.rs`, `agent/doctor.rs` for
   `println!/eprintln!/eprint!/print!/io::stdout/io::stderr` → ZERO matches. Decision logic in
   engine, printing/replace/`[y/N]` in CLI. (Pre-existing `addrepo.rs` prints are not from this branch.)
4. **Destructive op fail-closed + dry-run default (self uninstall)** — PASS.
   `self_uninstall.rs`: `dry_run = !spec.apply` (L77); ALL `fs::remove_*` behind `if spec.apply`
   (L107-133) → no flag ⇒ zero writes; binary-removal guard computes `current_exe()` file-stem ∈
   {envctl, envctl-gui} BEFORE any write (L92-105) and double-gates removal at L124, recording
   `refused` otherwise. Refusal-path unit test
   `preview_writes_nothing_and_guard_refuses_non_envctl_binary` (self_uninstall.rs:271/285) asserts
   dry-run + zero writes + guard refusal. Proven.
5. **CLI+GUI parity (agent doctor)** — PASS (see Parity check). The one REQUIRED-parity item is
   genuinely dual-front-end and drives the identical `Engine::agent_doctor`.
6. **Rust-native, no drift** — PASS. `develop..HEAD` carries NO new `.js/.ts/.py/.go/.c/.cpp`
   source; TASK-0019 delta is Rust + Cargo.{toml,lock} + the new `.sh` gate + `ci.yml`. Sole
   accepted divergence = baby-mimalloc(Rust) for kasetto's mimalloc(C), verified C-free.
7. **Lock honesty** — PASS. No envctl.lock/agent-env.lock/manifest mutation in this delta; KDF
   feature touches only `crates/secrets-engine/Cargo.toml` + `keyslot.rs` + the gate + `ci.yml`.
8. **Engine non-printing not regressed by commits 2-5** — PASS. Commit 2 (doctor config-less) is
   read-only (no guard touched, no dep); commit 3 (short-forms) is CLI clap aliases only; commit 4
   (test serialization) is test-only (`#[cfg(test)] test_env_lock`); commit 5 (KDF) is cfg
   constants + test + gate. clippy+both test runs cover all.

## Env-test race fix (commit 4) — re-confirmed
Shared poison-resilient `crate::test_env_lock()` defined once at `engine/src/lib.rs:237`. The three
env-touching test sites acquire it: `update_notifier.rs:152` (local `env_lock` delegates to it),
`agent/doctor.rs:324` + `:393` (the two HOME/XDG/cache-mutating doctor tests), and
`agent/init.rs:130` (`init_path_global`). Doctor tests restore prior env values (not blank
remove_var). Both full test runs green with default parallel threads — race resolved.

## Parity check (Engine method → CLI caller / GUI caller)
- `Engine::agent_doctor` (`engine/src/agent/doctor.rs:35`)
  - CLI: `crates/cli/src/main.rs:1541` `AgentResult::Doctor(eng.agent_doctor(spec, &sink)?)`.
  - GUI: `crates/engine/src/command.rs:255` worker `AgentCommandSpec::Doctor(s) => engine.agent_doctor(s, &sink)`;
    GUI builds the spec at `gui/src/main.rs:1350` (`agent_doctor_spec`) and renders the report at
    `gui/src/main.rs:1685` (`agent_doctor_tables`). **Both front-ends drive the identical method.**
- All other TASK-0019 items (completions / self update+uninstall / notifier / global flags /
  `--frozen` / short-forms) are CLI-only with documented justifications (clap-tree introspection,
  self-replacing running binary, end-of-run terminal concept, terminal presentation) — accepted per plan.

## Findings
None blocking. Informational notes (carried, not findings):
- `ci/gates/kdf-feature-off.sh` depends on `python3` (same as `no-c.sh`); CI runners and this host
  have it. Fail-closed on metadata/parse error — correct.
- The `develop..HEAD` diff is large because the worktree base is ahead of the current `develop`
  (proto/systemd/yazelix files); the 5-commit delta itself is exactly the files in the commit stats.

## Re-test needed
None. If a fix lands later, re-run (raw, via `rtk proxy` / direct bash):
```
bash ci/gates/no-c.sh; echo exit=$?
bash ci/gates/kdf-feature-off.sh; echo exit=$?
rtk proxy cargo clippy --workspace -- -D warnings; echo exit=$?
rtk proxy cargo test --workspace; echo exit=$?                                            # default (full-cost) — production_floor_constants_pinned must be ok
rtk proxy cargo test --workspace --features envctl-secrets-engine/low-cost-kdf-tests; echo exit=$?
rtk proxy cargo build --release -p envctl-secrets-engine --features low-cost-kdf-tests; echo exit=$?  # MUST FAIL (101)
rtk proxy cargo build -p envctl-gui; echo exit=$?
```
