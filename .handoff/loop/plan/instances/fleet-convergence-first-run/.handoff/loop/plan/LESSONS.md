# LESSONS — planning-engineer harness (durable, append-only)

One row per **generalized** lesson (the CLASS of problem, not the instance). `recurrence` is the count
of cycles in which this class has appeared; a class seen **twice** escalates from *noted* to
*upgrade-now*. Never truncate this file — recurrence history is the point.

Columns: `date · harness · lesson (class) · evidence · recurrence · routed-to · status`.

---

| date | harness | lesson (CLASS) | evidence | recurrence | routed-to | status |
|---|---|---|---|---|---|---|
| 2026-06-26 | planning-engineer | **L1 — Producer artifact NAMES must share a single source of truth with the completeness gate.** When a generator (eject / cartographer / researcher) and the artifact-gate independently encode the same required filenames/section-headers, they drift, forcing mid-cycle reconcile round-trips. The producer should be driven by — or self-checked against — the same name/header list the gate asserts. | `reports/agent-run-ledger-rusty-idd.md:32-35` (agents 1+2 resumed via SendMessage to emit gate-named graph JSON splits + the trends `"Tool-currency & advisories"`/`"Sources"` headers); gate-required names at `scripts/plan-artifact-gate.sh:155-157,187`. The CLASS fired **twice in one cycle** (two distinct producers). | 1 (intra-cycle ×2 — systemic; **cycle 2: did NOT recur**) | `plan-cartography` SKILL + `plan-trend-research` SKILL (producer-side self-check) and/or bundled `scripts/` name-contract shared with `plan-artifact-gate.sh` | **VALIDATED** — cycle 2 every lane emitted gate-named artifacts with **0 SendMessage reconciles** (`reports/agent-run-ledger-handoff.md:9-23`); the behavioral fix works. Durable skill-edit (P1) still **APPLY-when-approved**. |
| 2026-06-26 | planning-engineer | **L2 — A verifier-gated state ledger needs an explicit flip-authority + flip-rule.** When a dimension/status ledger has `[~]`→`[x]` marks but the skill never states WHO flips and on WHAT signal, the verifier stays conservative and the orchestrator must reconcile. Assign: verifier flips on CONFIRMED; orchestrator flips only the documented residual. | `dimensions.md` (6 dims left `[~]` though their claims are CONFIRMED in `findings/verdicts.md`); no flip-rule in the verifier/orchestrator skills. | 1 | `plan-loop` orchestrator SKILL + `plan-verifier` agent def | proposed |
| 2026-06-26 | planning-engineer | **L3 — A node-per-backlog-slug completeness gate is correct; scale the ARTIFACT, never the gate.** As the backlog grows (63 slugs → 63 DAG nodes / 30 edges) the human-readable DAG md gets heavy. The fix is representation (md may summarize; the machine companion JSON stays exhaustive and remains the gated source of truth), NOT loosening the one-node-per-slug rule. | `graph/target-dag.md:8-11` (63 nodes, paired `graph/target-dag.json`); gate enforces a node per `targets.md` slug at `scripts/plan-artifact-gate.sh:121-142`. | 1 | `plan-dependency-graph` SKILL (artifact representation only; gate unchanged) | proposed |
| 2026-06-26 | planning-engineer | **L4 — Every upgrade row that reaches the PLAN (promoted, not candidate) must carry a feasibility verdict.** This cycle only the architecture set + DC-2/DC-5/FL-3 were feasibility-gated; per-axis upgrades reached the report as candidates. The architect labelled them honestly and the gate held — so STRENGTHEN the gate to assert the promoted/candidate distinction, locking in correct behavior. | `findings/verdicts.md:69-72` (only architecture + 3 cross-dim upgrades feasibility-gated); `reports/rusty-idd-plan.md:440-444` (other axes reported as candidates, not promoted). | 1 (**cycle 2: improved** — all 39 upgrade rows carry an explicit feasible/infeasible verdict, `verdicts.md` tallies) | `plan-artifact-gate.sh` (gate STRENGTHEN) + `plan-synthesis` SKILL (promoted-vs-candidate labelling) | proposed |
| 2026-06-26 | planning-engineer | **L5 — Inherited framing about a SECURITY- or ARCHITECTURE-critical mechanism must be re-derived from source, never propagated.** A seed/doc/trends/prior-cycle description of a crypto/safety/contract mechanism is an unverified claim until read at the source line. This cycle the inbound framing called the witness chain "blake3+ed25519-signed"; source proves it is SHAKE-256 hash-linked + UNSIGNED (blake3 is used elsewhere, for `work-order` intent_lock). The *class* — verifier corrects asserted/inherited framing against source — already fired in cycle 1 (merge.rs path + spec_* count corrections), so it RECURS → escalate to upgrade-now. | `findings/verdicts.md:93` (EXP-3: `RuVector/.../witness.rs:74 shake256_256`; `ledger/src/v1.rs:20` imports no `sign`); `verdicts.md` Tallies "Correction propagated"; cycle-1 recurrence: cycle-1 evaluation §Gate-quality (C13 path + spec_* count QUALIFIED corrections). | 2 (cycle 1 citation-correction + cycle 2 SHAKE-256) — **upgrade-now** | `plan-verifier` agent def + `plan-trend-research` SKILL (do NOT propagate seed/doc crypto/safety framing; security-critical claims require a source-line derivation) | proposed (P7) |
| 2026-06-26 | planning-engineer | **L6 — A "standalone / portable-root residency" claim must be proven by a clean-clone / standalone build, never inferred from the in-repo crate graph.** A `../../sibling` path-dep passes a crate-graph read (the manifest exists at author time) yet fails the moment the repo is cloned alone — it breaks the portable-root residency mandate silently. Make an empirical clean-clone/standalone `cargo build` a STANDING verifier experiment for any target that claims standalone residency, and gate it (CI + plan-artifact-gate). | `findings/verdicts.md:91` (EXP-1: `cargo build -p ledger` AND `--features redb-store` both fail at manifest-load on `../../RuVector/.../rvf-crypto/Cargo.toml`); `reports/handoff-plan.md:18,208` (the #1 blocker); `dimensions.md` filesystem-layout fs-1. | 1 | `plan-verifier` agent def (standing standalone-build experiment) + clean-clone build gate (CI + `plan-artifact-gate.sh`) | proposed (P5) |
| 2026-06-26 | planning-engineer | **L7 — A mirrored cross-repo CONTRACT (file-copied schema, not a dependency) is a silent drift seam; planning a union/merge must require a cross-repo drift check.** `rusty-idd`'s `work-order` is a *contract mirror* of handoff's `task.schema.json`, not a path-dep — so the two can diverge with nothing to catch it, and the union inherits the fail-open loader through the mirror. When the harness plans a port-and-merge and finds a mirrored contract, it should require a differential-golden drift artifact across both repos. | `findings/verdicts.md:110` (A-C13 "a *contract mirror*, not a path-dependency", `rusty-idd/.../work-order/src/lib.rs:35,249`); `verdicts.md:159` (ts-3 union inherits fail-open via mirror); ts-U4 golden parity = the fix. | 1 | `rust-port-merge` / `cross-repo-referencer` SKILL (require a cross-repo schema-drift/golden artifact when a mirrored contract is found) | proposed (P6) |
