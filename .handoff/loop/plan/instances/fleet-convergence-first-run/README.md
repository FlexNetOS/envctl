# Archived planning-instance shared artifacts — fleet-convergence-first-run

This directory preserves the branch-local versions of shared `.handoff/loop/plan/*`
artifacts that collided after parallel planning instance `plan/loop-weave` merged first.
The merge keeps the latest global/shared files from `origin/master` in place and stores
this instance's versions here so no planning evidence is lost while avoiding downgrades
or silent clobbering of the already-merged weave planning docs.
