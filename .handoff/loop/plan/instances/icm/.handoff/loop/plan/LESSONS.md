# LESSONS — fleet-convergence planning loop (append-only; recurrence-counted)

| id | cycle | lesson (class, generalized) | route | recurrence | status |
|----|-------|------------------------------|-------|------------|--------|
| L-icm-1 | 7 (icm) | Under a hard token ceiling, the cycle CLOSE (architect synthesis + steward + ship) can be done in FOREGROUND from the findings already in context — no architect/steward sub-agents needed. A "low-token close" mode preserves a near-complete cycle when budget runs out mid-flight. | skill body (add a "token-frugal close" note to plan-loop) | 1 (noted) | proposed |
| L-icm-2 | 7 (icm) | Cross-agent NUMERIC facts (dimensions, versions) drift and must be verifier-adjudicated before becoming plan facts — 3 agents repeated "384d" but the real default is 768d. The verifier's empirical probe caught it. Keep numeric claims in the "must-refute" set. | agent def (verifier) / orchestrator | 2 (RECURRENCE — also seen as the prompt_hub 384/Qdrant figures) | **apply-eligible** |
| L-icm-3 | 7 (icm) | Self-referential sentinel tokens in real filenames (TODO.md etc.) trip the gate's placeholder regex; auditors must hyphen-break real tokens. (Carried from cycle 6.) | bundled gate / filesystem-auditor prompt | 2 (RECURRENCE) | applied prior cycle |
| L-icm-4 | 7 (icm) | "Bound as data vs invoked by convention" is the recurring convergence test for every organ — icm (MCP convention), prompt_hub (prose goal-artifact), grit (no Reconcile cmd). The bind-as-DATA contract is the consistent GREEN target shape. | skill body / architect | 3 (pattern) | proposed |

See `proposed-upgrades.md` for structural items (PROPOSE-only; never weaken a gate).
