# LESSONS — planning-engineer harness (parallel weave instance, cycle 4)

One row per **generalized** lesson (the CLASS of problem, not the instance). `recurrence` is the count
of cycles in which this class has appeared; a class seen **twice** escalates from *noted* to
*upgrade-now*. Append-only — never truncate; recurrence history is the point.

L1–L7 live in the union-loop ledger
(`.worktrees/plan-fleet-convergence/envctl/.handoff/loop/plan/LESSONS.md`) and the durable harness
ledger (`envctl/LESSONS.md`). This file appends cycle-4 lessons L8–L10 from the parallel weave instance
and continues the same numbering; L1, L5 recurrence is updated where this cycle bears on them.

Columns: `date · harness · lesson (CLASS) · evidence · recurrence · routed-to · status`.

---

| date | harness | lesson (CLASS) | evidence | recurrence | routed-to | status |
|---|---|---|---|---|---|---|
| 2026-06-26 | planning-engineer | **L8 — Lease + own-worktree gives true parallel isolation; the parallel-run design is validated.** Running a per-target loop instance in its own git worktree on its own branch under a per-target HF lease lets it fan out 13 agents and emit a full plan with **zero** edits to a sibling/union loop branch. The isolation primitive (worktree + branch + lease) is sufficient for concurrent loop instances — no shared-state contention observed. | `loop_state.md:13-14` (own worktree `plan/loop-weave` + lease `plan:claim:weave`); `reports/agent-run-ledger-weave.md:27-30` parallel-isolation proof ("zero edits to the union loop branch"); first run of `prompt_hub/prompts/plan-loop-parallel-run.md` #181. L1 artifact-name contract also held under parallelism (0 SendMessage reconciles in the ledger). | 1 (validates the parallel design) | `plan-loop` orchestrator SKILL (parallel-run mode) — **validated, no edit required**; the state-namespacing polish is the only follow-up (P8) | noted (design validated) |
| 2026-06-26 | planning-engineer | **L9 — Under parallel fan-out, a not-yet-present PEER artifact is "pending", not "missing".** When concurrent axis agents read each other's sibling artifacts, one can observe a peer's file before that peer lane has finished writing it and momentarily read it as absent. Treating that as a fail-closed "missing artifact" finding would ship a false negative. The correct stance: a peer artifact that is expected-but-absent during concurrent execution is PENDING (re-check after orientation/peer completion), and a hard fail-closed finding is reserved for an artifact still absent AFTER the producing lane is known complete. | This cycle: a transient concurrency-timing read where an axis lane saw a sibling artifact "not yet present"; **no false "missing" finding reached `verdicts.md`** because the orchestrator already gates analysis on orientation completion (so the transient read self-resolved). evaluation.md §Friction. | 1 | `plan-analyst` / axis-auditor agent prompts — add a "peer-artifact pending-vs-missing" rule; note the orchestrator's orientation-completion gate already prevents the false finding (defense already present) | proposed (P9) |
| 2026-06-26 | planning-engineer | **L10 — "Documented gate undercounts the real gate" is the framing-vs-source class (L5) again.** A human/machine policy doc can state a weaker count than the gate actually enforces; the enforced gate is the source of truth and the doc is a stale restatement. This cycle: `sync-master.yml:46` enforces **7** required CI checks (incl. the teeth-bearing supply-chain `audit`) while `CLAUDE.md:43` and `policy.toml:37` both list **6** (GOV-003). This is the SAME CLASS as L5 (inherited/asserted framing about a security-/contract-critical mechanism must be re-derived from source, never propagated) — here the framing is a count of *enforced gates*, which is itself a contract surface. The class recurs → stays upgrade-now. | `verdicts.md:31,56` (GOV-003: `sync-master.yml:46`=7 incl. `audit`; `CLAUDE.md:43` & `policy.toml:37`=6); routes to U-GOV-001 (align doc 6→7). Companion GOV-004: `ci.yml:69,181` run Python while the invariant says no-Python — same "doc/invariant vs enforced reality" drift. | 3 (L5 class: cycle-1 citation-correction + cycle-2 SHAKE-256 + cycle-4 CI-gate-count) — **upgrade-now** (already P7) | `plan-verifier` agent def (source-derive security-/contract-critical framing) + `plan-trend-research` SKILL — bump P7 recurrence; **add CI-gate-count / enforced-vs-documented to P7's claim class** | proposed (P7, recurrence bumped) |
| 2026-06-26 | planning-engineer | **L1 recurrence (no new upgrade): artifact-name contract held under parallelism.** The producer↔gate artifact-name drift class (L1) that cost 2 reconcile round-trips in union cycle 1 produced **0** reconciles this cycle even with 13 concurrent lanes. The behavioral fix continues to hold; the durable skill-edit (P1) remains APPLY-when-approved. | `reports/agent-run-ledger-weave.md` (13 lanes, no SendMessage-reconcile rows); contrast union cycle-1 evaluation §Friction (2 reconciles). | 1 (intra-cycle ×2 union c1; cycle-2 + cycle-4 weave: did NOT recur) | `plan-cartography`/`plan-trend-research` SKILL — P1 (APPLY-when-approved) | noted (validated again) |
