# icm — governance/settings/config findings (cycle 7)

axis: **governance+settings+config** | target_root: /home/drdave/Desktop/meta/icm | READ-ONLY on icm code.
Frame: meta = ONE converging system; icm = persistent-memory organ binding into the handoff+rusty-idd
north-star. This file audits icm's control plane + settings/config plane.

## Surfaces scanned
| surface | path(s) | present | evidence |
|---|---|---|---|
| rules `.claude/rules/*.md` | /home/drdave/Desktop/meta/icm/.claude/ | NO | `find .claude -type f` → only rusty-idd-adapter.md, scheduled_tasks.lock, settings.json |
| skills/agents `.claude/skills,agents` | /home/drdave/Desktop/meta/icm/.claude/ | NO | dir absent — no harness organ in icm |
| instructions AGENTS.md | /home/drdave/Desktop/meta/icm/AGENTS.md | YES (degenerate) | AGENTS.md:1-32 — ENTIRE file is the auto-injected `<!-- icm:start -->`…`<!-- icm:end -->` ICM stanza; no mission/hard-rules/fail-closed law/rusty-idd pointer |
| instructions CLAUDE.md | /home/drdave/Desktop/meta/icm/CLAUDE.md | YES (stale) | CLAUDE.md:1-16 French design doc; describes Turso/libsql + 1536-d OpenAI — not the shipped stack |
| rusty-idd adapter | /home/drdave/Desktop/meta/icm/.claude/rusty-idd-adapter.md | YES | adapter.md:3 "GENERATED by `rusty-idd render claude`"; :5 ".claude/ is a THIN ADAPTER (ADR-0010/0015)" |
| rusty-idd control plane `.idd/`, `openspec/` | /home/drdave/Desktop/meta/icm/ | **NO** | `ls .idd openspec` → absent — adapter+hook reference a plane that is not in this repo |
| hooks (.claude/settings.json) | /home/drdave/Desktop/meta/icm/.claude/settings.json | YES (1 hook) | settings.json:3-14 SessionStart → `rusty-idd next --base $root` |
| hooks (.handoff/, .githooks/, preflight) | /home/drdave/Desktop/meta/icm/ | NO | no .handoff, no .githooks, no scripts/preflight.sh in icm (scripts/ is bench-only) |
| policy (.handoff/policies, ADRs, PILOT.toml) | /home/drdave/Desktop/meta/icm/ | NO | no .handoff/policies, no docs/adr-*.md (docs/ = architecture/features/guide/product) |
| runtime settings .claude/settings.json | /home/drdave/Desktop/meta/icm/.claude/settings.json | YES | hooks-only; no permissions/env/model/MCP block (icm is the MCP *server*, not a host config) |
| settings.local.json / .codex / agent-guard | /home/drdave/Desktop/meta/icm/ | NO | none present |
| app config | /home/drdave/Desktop/meta/icm/config/default.toml | YES | default.toml:1-84 store/memory/embeddings/extraction/recall/wakeup/mcp |
| Cargo workspace | /home/drdave/Desktop/meta/icm/Cargo.toml | YES | Cargo.toml:1-77 4-member workspace; rusqlite bundled :19 |
| toolchain pin rust-toolchain.toml | /home/drdave/Desktop/meta/icm/ | **NO** | `ls rust-toolchain*` absent; CI pins `dtolnay/rust-toolchain@stable` (ci.yml:22,33) |
| .cargo/config.toml | /home/drdave/Desktop/meta/icm/.cargo/ | NO | absent |
| CI/release | /home/drdave/Desktop/meta/icm/.github/workflows/*.yml | YES | ci.yml, cd.yml, next-release.yml, pr-target-check.yml, release.yml |
| supply-chain config (deny.toml/dependabot/SECURITY.md) | /home/drdave/Desktop/meta/icm/ | **NO** | `ls deny.toml .github/dependabot.yml SECURITY.md` all absent |
| RTK filters | /home/drdave/Desktop/meta/icm/.rtk/filters.toml | YES (template) | filters.toml:1-14 schema_version=1, only commented examples |
| MCP host injectors | /home/drdave/Desktop/meta/icm/install.sh, plugins/ | PARTIAL | install.sh:217-218 defers to `icm init` (Rust); plugins/ holds ONLY opencode-icm.ts |
| meta membership | /home/drdave/Desktop/meta/.meta.yaml:189-191 | YES | `icm:` repo+`provides: [icm]` |

## Hygiene detectors
| detector | result | evidence | risk |
|---|---|---|---|
| MCP rot | N/A — icm is the MCP *server*, not an MCP host; no `.codex/config.toml [mcp_servers.*]` in repo | `ls .codex` absent; transport=stdio config/default.toml:78-80 | low |
| Skill overload | none — icm ships 0 skills/agents (no `.claude/skills`); zero listing-budget burn from icm itself | `.claude` tree has no skills/agents | none (inverse risk: no harness organ — see gov-002) |
| Token burn | HIGH — CLAUDE.md is 26.2K of stale French prose + inlined source snippets, auto-loaded every session | `ls -la CLAUDE.md` 26.2K; head shows French design doc + struct dumps | high (every-session context tax for wrong info) |
| Permission drift | N/A — no permissions block; only 1 SessionStart hook spawning `rusty-idd next` | settings.json:3-14 | medium (hook runs unconditionally at session start; see gov-003) |
| Config drift (toolchain) | unpinned Rust toolchain → non-reproducible builds + silent clippy-lint drift when `stable` bumps | ci.yml:22,33 `@stable`; no rust-toolchain.toml | medium |
| Config drift (supply-chain) | bundled-C SQLite CVE blind spot; cargo-audit installed unpinned each run; no deny.toml/dependabot | ci.yml:67-68 `cargo install cargo-audit`; rusqlite bundled Cargo.toml:19 | HIGH |
| Config drift (docs↔stack) | CLAUDE.md documents Turso/libsql + `type="turso"` + 1536-d OpenAI; shipped stack is rusqlite-bundled + fastembed multilingual-e5 | CLAUDE.md:8,16,37,41,96,134,401-404 vs Cargo.lock:2654-2656 rusqlite 0.34, no libsql/turso in lock; config/default.toml has no `type` key | HIGH |
| Enforcement drift | adapter claims `rusty-idd render --check` is a CI gate; no CI workflow runs it | adapter.md:3 vs `grep -r rusty-idd .github/workflows` → none | medium (stated gate, no teeth) |

## CLAIM rows
- CLAIM[gov-001] axis: governance+settings+config | surface: CLAUDE.md | evidence: /home/drdave/Desktop/meta/icm/CLAUDE.md:8,16,37,401-404 vs /home/drdave/Desktop/meta/icm/Cargo.lock:2654-2656 (rusqlite 0.34, libsqlite3-sys 0.32, no libsql/turso) | confidence: High | The 26.2K auto-loaded CLAUDE.md is a stale French design doc describing a Turso/libsql + 1536-d-OpenAI architecture that the repo abandoned for rusqlite-bundled SQLite + fastembed (multilingual-e5-base, 384/768d). The primary control-plane instruction surface is materially wrong AND a per-session token tax.
- CLAIM[gov-002] axis: governance+settings+config | surface: AGENTS.md | evidence: /home/drdave/Desktop/meta/icm/AGENTS.md:1-32 | confidence: High | icm's AGENTS.md is ONLY the auto-injected ICM persistent-memory stanza — no mission, no hard rules, no fail-closed law, no pointer to rusty-idd/handoff. For the persistent-memory organ of a converging fabric, the governing instruction file carries zero repo-local governance; the harness organ (`.claude/skills`,`.claude/agents`) is also absent.
- CLAIM[gov-003] axis: governance+settings+config | surface: .claude (control-plane incoherence) | evidence: /home/drdave/Desktop/meta/icm/.claude/rusty-idd-adapter.md:3-5 + /home/drdave/Desktop/meta/icm/.claude/settings.json:7 + absence of /home/drdave/Desktop/meta/icm/.idd | confidence: High | Triple-incoherent control plane: (a) `.claude/` is a rusty-idd THIN ADAPTER, (b) SessionStart hook unconditionally runs `rusty-idd next --base $root`, yet (c) there is NO `.idd/`/`openspec/` plane in icm. The hook is wired to a control plane absent from the repo — fail-OPEN if `rusty-idd` is missing, error/no-op every session if present. Meanwhile CLAUDE.md (French design doc) and AGENTS.md (ICM stanza) neither mention rusty-idd: three instruction surfaces, three different stories.
- CLAIM[gov-004] axis: governance+settings+config | surface: ci.yml (supply-chain) | evidence: /home/drdave/Desktop/meta/icm/.github/workflows/ci.yml:67-83 + /home/drdave/Desktop/meta/icm/Cargo.lock:1736-1738 (libsqlite3-sys 0.32.0) + cycle-7 research advisory | confidence: High | The only supply-chain gate is `cargo audit`, which inspects crate advisories but is BLIND to the C SQLite that libsqlite3-sys 0.32 statically bundles (SQLite 3.49.1). That misses ≥4 upstream SQLite CVE fixes incl. in-window CVE-2026-11822 (FTS5 heap overflow, fixed 3.53.2). icm's FTS/BM25 path makes FTS5 exposure plausible. No deny.toml, no dependabot, no SECURITY.md, and cargo-audit is `cargo install`-ed unpinned each run.
- CLAIM[gov-005] axis: governance+settings+config | surface: toolchain pinning | evidence: /home/drdave/Desktop/meta/icm/.github/workflows/ci.yml:22,33,52,64 (`dtolnay/rust-toolchain@stable`) + absence of /home/drdave/Desktop/meta/icm/rust-toolchain.toml | confidence: High | No toolchain pin anywhere. CI rides whatever `stable` is on the run date; a `cargo clippy --workspace --all-targets -- -D warnings` (ci.yml:37) gate against a moving toolchain means a new clippy release can red the gate with zero source change — non-reproducible builds and lint-drift surprise blocks.
- CLAIM[gov-006] axis: governance+settings+config | surface: enforcement drift | evidence: /home/drdave/Desktop/meta/icm/.claude/rusty-idd-adapter.md:3 vs `grep -rniE 'rusty-idd|render --check' .github/workflows/` → no match | confidence: High | The adapter asserts "`rusty-idd render --check` (CI) fails on drift," but no icm CI workflow runs render --check. The committed `.claude/` adapter can silently drift from the (absent) rusty-idd engine with no gate — a stated control with no teeth.
- CLAIM[gov-007] axis: governance+settings+config | surface: install/plugin injectors | evidence: /home/drdave/Desktop/meta/icm/install.sh:217-218 + /home/drdave/Desktop/meta/icm/plugins/ (only opencode-icm.ts) | confidence: Medium | The multi-host injector surface (claude/zed/copilot-cli/continue/codex/opencode) is delegated to the `icm init` Rust binary; only the opencode plugin ships as a reviewable file in `plugins/`. The host-injection contract is therefore not auditable from config — its currency across hosts can drift invisibly (no test fixture per host in repo tree scanned).
- CLAIM[gov-008] axis: governance+settings+config | surface: feature-flag config | evidence: /home/drdave/Desktop/meta/icm/crates/icm-cli/Cargo.toml:16-27,45-47 | confidence: Medium | `default = ["embeddings","tui"]` makes the heavy fastembed ML stack + model download a DEFAULT build/runtime cost; and the `openssl`/`vendored-openssl` cross-build feature was "accidentally removed (PR #154), restored here" per the inline comment — a documented config-fragility scar in the release matrix with no guard test pinning the cross-build feature set.
- CLAIM[gov-009] axis: governance+settings+config | surface: committed lock artifact | evidence: `git ls-files .claude/scheduled_tasks.lock` → tracked | confidence: Medium | `.claude/scheduled_tasks.lock` is committed. A per-machine scheduler lock in version control is mutable runtime state masquerading as config; likely should be gitignored (alongside the existing `*.db`/`.env` ignores in .gitignore:1-5).

## UPGRADE rows
- UPGRADE[gov-001] axis: governance+settings+config | target_surface: /home/drdave/Desktop/meta/icm/CLAUDE.md | risk_tier: PROPOSE | rationale: replace the 26.2K stale French Turso design doc with a current, lean CLAUDE.md (real stack: rusqlite-bundled SQLite + sqlite-vec + fastembed; a coherent harness/rusty-idd pointer; change-history table) — kills both the misinformation and the per-session token tax | acceptance: `grep -ciE 'turso|libsql|1536|F32_BLOB' CLAUDE.md` == 0 AND file < 6KB AND it names rusqlite+fastembed | reversibility: git revert (docs-only) | blast: instruction surface only, no code | evidence: CLAIM[gov-001] | risk: low-content/high-value
- UPGRADE[gov-002] axis: governance+settings+config | target_surface: /home/drdave/Desktop/meta/icm/AGENTS.md | risk_tier: PROPOSE | rationale: add a repo-local governance preamble (mission as the persistent-memory organ, hard rules, fail-closed law, pointer to rusty-idd/handoff fabric) ABOVE the auto-injected ICM stanza, without touching the `<!-- icm:start/end -->` markers | acceptance: AGENTS.md contains a Mission + Hard-rules section AND the ICM stanza markers remain intact AND it names the handoff/rusty-idd north-star | reversibility: git revert | blast: instructions only | evidence: CLAIM[gov-002] | risk: low
- UPGRADE[gov-003] axis: governance+settings+config | target_surface: /home/drdave/Desktop/meta/icm/.claude/settings.json + .claude/rusty-idd-adapter.md | risk_tier: PROPOSE | rationale: resolve the control-plane incoherence — EITHER actually onboard icm to rusty-idd (add `.idd/` + a CI `rusty-idd render --check` gate so the adapter's claim is true) OR remove the rusty-idd thin-adapter+SessionStart hook and govern icm via its own AGENTS.md. Do not leave a hook pointed at an absent plane | acceptance: either (a) `.idd/` exists AND a CI job runs `rusty-idd render --check`, OR (b) the `rusty-idd next` SessionStart hook + rusty-idd-adapter.md are removed; in both cases no instruction surface references a non-existent plane | reversibility: git revert; owner-walled (touches hook + control-plane topology) | blast: every session start in icm | evidence: CLAIM[gov-003], CLAIM[gov-006] | risk: medium — owner decision (NEVER weaken: keep fail-closed)
- UPGRADE[gov-004] axis: governance+settings+config | target_surface: /home/drdave/Desktop/meta/icm/.github/workflows/ci.yml (+ new scripts/check-bundled-sqlite.sh) | risk_tier: PROPOSE | rationale: close the bundled-C CVE blind spot — add a step that resolves libsqlite3-sys → bundled SQLite version and fails/warns when it trails the latest CVE-fixed upstream (currently 3.49.1 vs ≥3.53.2 for CVE-2026-11822); add a pinned cargo-audit + a deny.toml/cargo-deny (bans+licenses) and dependabot for crate currency | acceptance: CI emits the bundled SQLite version AND flags it as stale vs a tracked floor AND deny.toml present AND cargo-audit version pinned | reversibility: workflow-only revert | blast: release supply-chain trust boundary | evidence: CLAIM[gov-004] | risk: medium — adds a gate, never relaxes one
- UPGRADE[gov-005] axis: governance+settings+config | target_surface: /home/drdave/Desktop/meta/icm/rust-toolchain.toml (new) | risk_tier: PROPOSE | rationale: pin the Rust toolchain channel so the `-D warnings` clippy gate and builds are reproducible and stable-bumps don't surprise-red CI | acceptance: rust-toolchain.toml exists with a pinned channel AND CI honors it (drop bare `@stable` or pin its `toolchain:`) | reversibility: delete file | blast: all CI jobs + local builds | evidence: CLAIM[gov-005] | risk: low-medium
- UPGRADE[gov-006] axis: governance+settings+config | target_surface: /home/drdave/Desktop/meta/icm/crates/icm-cli/Cargo.toml + release.yml | risk_tier: PROPOSE | rationale: add a CI guard pinning the cross-build feature set (the `openssl`/`vendored-openssl` regression of PR #154) so the release matrix can't silently lose it again; document/justify `embeddings` default-on (heavy fastembed) vs a lean default | acceptance: a CI job builds the cross-target feature combo that PR #154 broke AND fails if `vendored-openssl` is absent | reversibility: revert | blast: release cross-build matrix | evidence: CLAIM[gov-008] | risk: low
- UPGRADE[gov-007] axis: governance+settings+config | target_surface: /home/drdave/Desktop/meta/icm/.gitignore + .claude/scheduled_tasks.lock | risk_tier: APPLY | rationale: stop tracking the per-machine scheduler lock as config | acceptance: `.claude/scheduled_tasks.lock` is gitignored AND `git ls-files` no longer lists it | reversibility: trivial revert | blast: none (runtime state) | evidence: CLAIM[gov-009] | risk: low
- UPGRADE[gov-008] axis: governance+settings+config | target_surface: /home/drdave/Desktop/meta/icm/plugins/ + host-injector fixtures | risk_tier: PROPOSE | rationale: make the multi-host MCP injection contract auditable — ship/golden-test the per-host config templates (claude/zed/copilot-cli/continue/codex/opencode) that `icm init` writes, so injector drift across hosts is caught in CI rather than discovered by users | acceptance: a test/fixture exists per supported host asserting the injected MCP block shape | reversibility: revert | blast: install/onboarding surface | evidence: CLAIM[gov-007] | risk: low

## Gaps / owner walls
- OWNER WALL: gov-003 (rusty-idd onboard-or-remove) is a control-plane topology decision — auditor PROPOSES, owner decides; fail-closed posture must be preserved either way.
- OWNER WALL: gov-004 (supply-chain gates) and gov-005 (toolchain pin) add/strengthen gates only — never relax cargo-audit or clippy `-D warnings`.
- INCONCLUSIVE: gov-007 — full per-host injector currency is inside the `icm init` Rust binary (READ-ONLY on icm code this cycle); only opencode-icm.ts is file-visible. Confirming all six hosts' injected shapes requires reading icm-cli source (deferred; flagged not silently passed).
- N/A — MCP rot: icm is the MCP server, not a host; no `.codex/config.toml [mcp_servers.*]` exists in this repo to rot.
- N/A — bun/node config: icm is a pure-Rust workspace; no package.json/bun.lock to audit (plugins/*.ts + scripts/*.ts are standalone, not a bun project tree).
- N/A — .handoff policy/PILOT.toml: icm has no `.handoff/` of its own; its continuity binds via the meta-root handoff fabric, not a repo-local policy plane.
