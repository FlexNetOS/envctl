# prompt-hub — governance/settings/config findings

Target: `/home/drdave/Desktop/meta/prompt_hub` @ `f826ea33` (branch context: plan/lifeos-meta-front-door).
Axis: **governance+settings+config**. Read-only audit.

## Surfaces scanned
| surface | path(s) | present | evidence |
|---|---|---|---|
| instructions (Claude) | `CLAUDE.md` (12.5K) | yes | `CLAUDE.md:7,9,104` — Rust-native invariant + harness pointer + change history |
| instructions (multi-harness) | `AGENTS.md`, `AGENT_GUIDE.md`, `.agent.md`, `.instructions.md`, `.prompt.md`, `GEMINI.md`, `.junie/AGENTS.md`, `skills/junie/SKILL.md` | yes | `CLAUDE.md:13-14` explicitly catalogs these as "advisory, non-authoritative" foreign-harness files |
| rules (`.claude/rules/*.md`) | `.claude/rules/` | **absent** | `ls .claude/rules` → no such dir; only `.claude/agents/` + `.claude/skills/` + `settings.local.json` |
| Claude settings | `.claude/settings.json` | **absent** | only `.claude/settings.local.json` exists; no lifecycle/PreToolUse hooks at the Claude layer |
| Claude settings (local) | `.claude/settings.local.json` (481B) | yes | permissions.allow (13 entries) + `env.CLAUDE_CODE_MAX_OUTPUT_TOKENS=35000` |
| agents | `.claude/agents/*.md` | yes (7) | backlog-curator, continuity-steward, docs-scribe, evolution-steward, feature-architect, rust-implementer, verification-gate |
| skills | `.claude/skills/*` (5) + `skills/*` (3) | yes | `.claude/skills/`: feature-build, harness-evolution, lane-loop, prompt-loop, session-relay; top-level `skills/`: junie, prompt-hub-dev, security-remediation |
| hooks (handoff contract) | `.handoff/hooks/hooks.toml` (2.3K, schema `handoff.hooks.v1`) | yes (8 events) | SessionStart/TaskClaim/PreEdit/PostEdit/PreHandoff/SessionStop/PreSessionStart/PostMerge |
| hook scripts | `.handoff/hooks/loop-entry.sh` (2.7K), `session-end.sh` (1.0K) | yes | `session-end.sh` present but NOT referenced by any `hooks.toml` event |
| git hooks | `.githooks/pre-commit` | yes | thin shim → `scripts/code_review.sh`; skips gracefully if absent |
| policy (loop) | `.handoff/policy.toml` (schema `handoff.policy.v1`) | yes | `[remote]`/`[loop]`/`[merge]`/`[preflight]`/`[sync]`; fleet Tier-B member |
| policy (rules) | `.handoff/policies/rules.toml` (schema `handoff.policy.rules.v1`) | yes | fail-closed defaults + `[merge.protected_files]` + `[commands.blocked]` |
| runtime/build config | `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`, `.cargo/config.toml`, `.cargo/mutants.toml` | yes | workspace 3 crates, edition 2024 |
| audit gate | `.deny.toml` (schema v2) | yes | enforced by `.github/workflows/security.yml` `deny` job |
| CI | `.github/workflows/*.yml` (12 workflows) | yes | ci, security, rust_native_guard, mutation, qodana, audit_sync, + 4 AI workflows |
| `.codex/`, `.meta.yaml` (repo-local) | — | **absent** | not a registered meta member at repo root; offline build via vendored sources |
| vendored sources | `vendor/` | yes (31,070 tracked files) | `.cargo/config.toml` `[source.crates-io] replace-with = "vendored-sources"` |

## Hygiene detectors
| detector | result | evidence | risk |
|---|---|---|---|
| MCP rot | none-found | no `.codex/config.toml`, no `[mcp_servers.*]` in repo; MCP servers are user-global, out of repo scope | none in-scope |
| Skill overload | low | 5 `.claude/skills` + 3 `skills/` = 8 total; no `skillListingBudgetFraction` set (no settings.json) — but count is modest | low token burn |
| Token burn | medium | `CLAUDE_CODE_MAX_OUTPUT_TOKENS=35000` (settings.local.json); CI re-`cargo install`s tarpaulin/git-cliff/cargo-audit/cargo-deny/cargo-mutants every run; 12 workflows incl. 4 AI workflows calling external LLM APIs per PR | CI minute + LLM-call burn |
| Permission drift | **high** | `settings.local.json` allows `Bash(git push *)`, `Bash(gh pr merge *)`, `Bash(git commit *)` with **no deny block** and **no PreToolUse guard** (no `.claude/settings.json`); `git push *` permits `--force` despite `rules.toml [commands.blocked]` listing `git push --force` | force-push/destructive ops not gated at Claude layer |
| Config drift | medium | tracked binary DBs (`prompt-hub/prompthub.db`, `prompt-hub/test.db` 192KB each); 31,070-file vendor tree; `rust-toolchain.toml`=1.96.0 vs Cargo.toml MSRV=1.91.1 (documented split, not a bug); `session-end.sh` orphaned | repo bloat + supply-chain surface |

## CLAIM rows

- CLAIM[gov-001] axis: governance+settings+config | surface: tracked SQLite binaries | evidence: `git ls-files | grep '\.db$'` → `prompt-hub/prompthub.db` (192512B) and `prompt-hub/test.db` (192512B) are tracked despite `.gitignore:6 *.db` | confidence: High | note: root `prompthub.db` (204K on disk) is CORRECTLY ignored (`git check-ignore -v prompthub.db` → `.gitignore:8`); the two committed 192KB binaries under `prompt-hub/` were force-added or pre-date the ignore rule and bloat the repo + leak local CLI/test state into history.

- CLAIM[gov-002] axis: governance+settings+config | surface: `.github/workflows/external-ai-apis.yml`, `ai-safety-deployment.yml`, `ai-code-review.yml`, `multi-model-evaluation.yml` | evidence: `external-ai-apis.yml:70` `"content": "...${{ steps.diff.outputs.content }}"`, `ai-safety-deployment.yml:61` `...${{ steps.changes.outputs.diff }}`, `ai-code-review.yml:58` `...${{ steps.changes.outputs.diff_output }}` — untrusted PR diff interpolated directly into a `run:` shell `curl -d '{...}'` JSON body | confidence: High | risk: GitHub Actions script injection (attacker-controlled PR diff → shell). The team already fixed this exact class in `audit_sync.yml:64-71` (env var, never interpolated) and removed `tj-actions/changed-files` for GHSA-mrrh-fwg8-r2c3 — the AI workflows were not brought up to that standard.

- CLAIM[gov-003] axis: governance+settings+config | surface: `.handoff/policies/rules.toml [merge.protected_files]` vs `.cargo/config.toml` | evidence: `rules.toml` patterns protect `.github/**`, `**/Cargo.toml`, `**/*.lock`, `rust-toolchain.toml`, but NOT `vendor/**`; `.cargo/config.toml` `[source.vendored-sources] directory = "vendor"` makes `vendor/` the ACTUAL build inputs (replaces crates-io) | confidence: High | risk: a PR can mutate vendored crate source (the real compiled bytes) without tripping the protected-file gate; `cargo audit`/`cargo deny` inspect `Cargo.lock` metadata, not vendored file contents, so a tampered vendor file is not caught.

- CLAIM[gov-004] axis: governance+settings+config | surface: `.handoff/policies/rules.toml [merge.protected_files]` self-guardrail coverage | evidence: patterns protect `.claude/skills/prompt-loop/**` and `.claude/skills/session-relay/**` only; `.claude/agents/**`, `.claude/skills/harness-evolution/**`, `.claude/skills/lane-loop/**`, `.claude/skills/feature-build/**` are unprotected, despite the inline comment "A confused/compromised agent must not be able to rewrite its own guardrails" | confidence: High | risk: 7 agent definitions + 3 of 5 skills (incl. the evolution-steward that self-upgrades the harness) can be rewritten by an agent PR without gate.

- CLAIM[gov-005] axis: governance+settings+config | surface: `.claude/settings.local.json` + absence of `.claude/settings.json` | evidence: `settings.local.json` permissions.allow includes `"Bash(git push *)"`, `"Bash(gh pr merge *)"`, `"Bash(git commit *)"`, `"Bash(meta git *)"` with no `deny` array; no `.claude/settings.json` exists → no `PreToolUse` hook | confidence: High | risk: the `rules.toml [commands.blocked]` list (`git push --force`, `git reset --hard`, `rm -rf /`) is enforced only by `hf`'s merge gate, never at the Claude-Code tool layer; an agent session can force-push.

- CLAIM[gov-006] axis: governance+settings+config | surface: `.github/workflows/external-ai-apis.yml` trigger gating | evidence: `external-ai-apis.yml:16-21` places `if: contains(github.event.pull_request.labels.*.name, 'ai-review')` UNDER `on.pull_request` — GitHub ignores `if` keys under `on:` (only valid at job/step level) | confidence: High | risk: the `ai-review` label gate is inert; the workflow's only real gate is the job-level `vars.ENABLE_AI_WORKFLOWS == 'true'` (`:33-38`) — currently fail-safe (skips green when unset), but the label intent is silently non-functional.

- CLAIM[gov-007] axis: governance+settings+config | surface: `vendor/` tree | evidence: `git ls-files vendor/ | wc -l` → 31,070 tracked files; introduced by PR #178 "vendor all external crates for self-contained offline builds" (`51c4afc6`); `.cargo/config.toml` replaces crates-io with `vendored-sources` | confidence: High | note: massive repo bloat + audit surface; the prompt's referenced revert PR #179 is NOT present in the local history (only #178 is merged here) — vendor-vs-revert policy is unsettled. `cargo publish --dry-run` already needs an escape hatch (`ci.yml:169` `--registry crates-io`) because vendoring breaks publish.

- CLAIM[gov-008] axis: governance+settings+config | surface: `scripts/check_safety.sh` vs AGENTS.md hard rule | evidence: `check_safety.sh:5,28` greps only `prompt-hub/src/` for `#![forbid(unsafe_code)]` and `unsafe` usage, while `AGENTS.md:282` states "No `unsafe` code anywhere" and the workspace has 3 crates (`prompthub`, `prompthub-server` unchecked) | confidence: High | risk: the no-unsafe invariant (a stated hard rule) is unenforced for 2 of 3 workspace members.

- CLAIM[gov-009] axis: governance+settings+config | surface: `.handoff/hooks/hooks.toml` vs deployed scripts | evidence: `.handoff/hooks/session-end.sh` (1.0K) exists but no `hooks.toml` event invokes it — `SessionStop` uses an inline command (`hf checkpoint --auto && ... hf fleet render prompt_hub`) | confidence: High | risk: orphaned hook script (drift between deployed scripts and the event contract); a future reader may assume `session-end.sh` runs at session end when it does not.

- CLAIM[gov-010] axis: governance+settings+config | surface: CLAUDE.md harness pointer vs real `.claude/skills/` | evidence: `CLAUDE.md:122-128` names skills `prompt-loop`, `feature-build`, `session-relay`, `harness-evolution` (+ agents) but omits `lane-loop`, which exists at `.claude/skills/lane-loop/` | confidence: Medium | risk: skill-with-no-pointer drift; `lane-loop` is undocumented in the harness change-history, so its provenance/ownership is opaque.

- CLAIM[gov-011] axis: governance+settings+config | surface: `Cargo.toml` / `rust-toolchain.toml` / `ci.yml` toolchain coherence | evidence: `Cargo.toml:8` `rust-version = "1.91.1"` (MSRV); `rust-toolchain.toml:3` `channel = "1.96.0"` (dev pin); `ci.yml:66` matrix `rust: [stable, 1.91.1]` tests the MSRV; `CLAUDE.md:7` documents the split | confidence: High | note: NOT drift — the prompt's premise of "pin 1.91.1" is the MSRV; the actual dev toolchain pin is 1.96.0, and CI exercises both stable and the 1.91.1 floor. Coherent across files. Edition 2024 confirmed (`Cargo.toml:7`).

- CLAIM[gov-012] axis: governance+settings+config | surface: model currency in AI workflows | evidence: `external-ai-apis.yml:64` `"model": "claude-opus-4-1"` (bare alias, Anthropic API); `multi-model-evaluation.yml:101` / `ai-code-review.yml:110` `"model": "claude/claude-opus"` (GitHub Models inference) | confidence: Medium | note: `claude-opus-4-1` is an older alias and `claude/claude-opus` is a non-versioned GitHub-Models id; both risk silent model EOL/redirect. Anthropic API call uses `secrets.ANTHROPIC_API_KEY` via `env:` (`:56-57`) — correct injection, not inline. The GitHub-Models workflows use only `GITHUB_TOKEN` (no third-party secret) — good.

- CLAIM[gov-013] axis: governance+settings+config | surface: `.deny.toml` strictness | evidence: `.deny.toml` `[bans] multiple-versions = "warn"` (not deny); `[advisories] ignore = []` (clean); `[licenses]` allow-list + one documented `option-ext`/MPL-2.0 exception; `[sources] unknown-registry/unknown-git = "deny"` | confidence: High | note: audit gate is well-formed and enforced via `security.yml`; only soft spot is multiple-versions being a warning, which a 31k vendored tree can quietly accumulate.

## UPGRADE rows

- UPGRADE[gov-001] axis: governance+settings+config | target_surface: `prompt-hub/prompthub.db`, `prompt-hub/test.db` | risk_tier: PROPOSE | rationale: 384KB of binary SQLite committed against the repo's own `*.db` ignore; leaks local CLI/test state into history and bloats clones | acceptance: `git ls-files | grep '\.db$'` returns only `.handoff/ledger.db` (the one intentionally-tracked, un-ignored ledger) and the vendored libsql test fixtures; `git rm --cached prompt-hub/prompthub.db prompt-hub/test.db` lands, tests regenerate them locally | reversibility: high (history rewrite not required; just untrack) | blast-radius: tests that read a pre-seeded DB must seed it in-test | evidence: CLAIM[gov-001]

- UPGRADE[gov-002] axis: governance+settings+config | target_surface: `.github/workflows/{external-ai-apis,ai-safety-deployment,ai-code-review,multi-model-evaluation}.yml` | risk_tier: PROPOSE | rationale: untrusted PR diff interpolated into shell — script-injection (the exact class the repo already remediated in `audit_sync.yml`) | acceptance: no `${{ steps.*.outputs.* }}` appears inside any `run:` body in these files; PR-diff content is passed via `env:` and read as `"$VAR"` (and JSON-built with `jq --arg`, never string-spliced); a PR whose diff contains `";id;"` produces no shell execution | reversibility: high | blast-radius: 4 workflows, AI-review path only (CI gates untouched) | evidence: CLAIM[gov-002]

- UPGRADE[gov-003] axis: governance+settings+config | target_surface: `.handoff/policies/rules.toml [merge.protected_files]` | risk_tier: PROPOSE | rationale: vendored sources are the real build inputs but are unprotected; agent guardrails (agents + 3 skills) are unprotected | acceptance: `patterns` adds `vendor/**`, `.claude/agents/**`, `.claude/skills/**` (or explicit harness-evolution/lane-loop/feature-build); a PR touching `vendor/<x>/src/lib.rs` or `.claude/agents/evolution-steward.md` trips `block_protected_file_changes` | reversibility: high | blast-radius: tightens the merge gate (never weakens it); legitimate dep bumps already go through `**/Cargo.toml`+`**/*.lock` review | evidence: CLAIM[gov-003], CLAIM[gov-004] | NOTE: strengthening only — owner-walled per "never weaken a guard."

- UPGRADE[gov-004] axis: governance+settings+config | target_surface: `.claude/settings.json` (new) + `.claude/settings.local.json` | risk_tier: PROPOSE | rationale: destructive-command guard exists in `rules.toml` but has no PreToolUse teeth at the Claude layer; `git push *` allows force-push | acceptance: a committed `.claude/settings.json` adds a `permissions.deny` (or PreToolUse hook) blocking `git push --force`, `git reset --hard`, `git clean -fdx`, `rm -rf` on repo paths — mirroring `rules.toml [commands.blocked]`; narrow `Bash(git push *)` to `--force-with-lease`-safe usage | reversibility: high | blast-radius: agent sessions only; CI unaffected | evidence: CLAIM[gov-005] | NOTE: relaxations forbidden; this only adds denies (owner-walled).

- UPGRADE[gov-005] axis: governance+settings+config | target_surface: `.github/workflows/external-ai-apis.yml` trigger | risk_tier: PROPOSE | rationale: the `ai-review` label gate is inert (mis-placed `if` under `on:`) | acceptance: label gating is expressed at the job-level `if:` (`contains(github.event.pull_request.labels.*.name, 'ai-review')`) alongside the existing `vars.ENABLE_AI_WORKFLOWS` check, and removed from `on.pull_request`; a PR without the label and without the var does not start the job | reversibility: high | blast-radius: one workflow's trigger logic | evidence: CLAIM[gov-006]

- UPGRADE[gov-006] axis: governance+settings+config | target_surface: `scripts/check_safety.sh` | risk_tier: PROPOSE | rationale: the "no unsafe anywhere" hard rule (`AGENTS.md:282`) is enforced on only 1 of 3 crates | acceptance: `check_safety.sh` scans `prompthub/src/` and `prompthub-server/src/` in addition to `prompt-hub/src/`; running it on a crate missing `#![forbid(unsafe_code)]` exits non-zero | reversibility: high | blast-radius: the `Safety Check` CI job (`ci.yml:171-177`) | evidence: CLAIM[gov-008]

- UPGRADE[gov-007] axis: governance+settings+config | target_surface: `vendor/` policy decision | risk_tier: PROPOSE | rationale: 31,070 vendored files is an unsettled policy (PR #178 vendored; prompt cites a #179 revert not present locally) — bloat + audit surface that bypasses `cargo audit`'s file-level visibility | acceptance: a recorded ADR under `.handoff/decisions/` states the offline/vendor stance (keep vendored + add `vendor/**` to protected_files + a `cargo vendor --locked` regen check, OR revert to crates-io + a fetch-cache); whichever is chosen, `git ls-files vendor/ | wc -l` matches the decision and `.cargo/config.toml` source-replacement is consistent | reversibility: medium (revert is a large diff) | blast-radius: every build (`[source.crates-io]` replacement) + `cargo publish` path | evidence: CLAIM[gov-007]

- UPGRADE[gov-008] axis: governance+settings+config | target_surface: AI-workflow model ids | risk_tier: APPLY | rationale: bare/aliased model ids (`claude-opus-4-1`, `claude/claude-opus`) risk silent EOL | acceptance: model ids are pinned to dated/explicit identifiers and centralized (workflow `env:` or repo var) so a single edit updates all AI workflows; CI lint or a comment documents the chosen models | reversibility: high | blast-radius: AI-review comment quality only (non-gating workflows) | evidence: CLAIM[gov-012]

- UPGRADE[gov-009] axis: governance+settings+config | target_surface: `.handoff/hooks/hooks.toml` ↔ `session-end.sh` + `CLAUDE.md:122-128` ↔ `lane-loop` | risk_tier: APPLY | rationale: orphaned `session-end.sh` and an undocumented `lane-loop` skill are low-risk doc/contract drift | acceptance: either `hooks.toml` `SessionStop` calls `session-end.sh` (or the script is removed), AND the CLAUDE.md harness pointer + change-history list `lane-loop` (or it is removed); `diff <(ls .claude/skills) <(skills named in CLAUDE.md)` is empty | reversibility: high | blast-radius: docs + non-gating hook wiring | evidence: CLAIM[gov-009], CLAIM[gov-010]

## Gaps / owner walls
- The prompt's premise that the toolchain pin is "1.91.1" is inaccurate: 1.91.1 is the **MSRV** (`Cargo.toml:8` + CI floor `ci.yml:66`); the dev pin is **1.96.0** (`rust-toolchain.toml:3`), and the split is documented (`CLAUDE.md:7`). No drift here — recorded as CLAIM[gov-011] for the record.
- The prompt's referenced **vendor-revert PR #179** is NOT in the local branch history (only #178 is merged); the revert may be unmerged or on another ref. Vendor policy is therefore inconclusive from this worktree — flagged as the open decision in UPGRADE[gov-007].
- MCP rot is **not assessable in-repo**: no `.codex/config.toml`/`[mcp_servers.*]` lives in `prompt_hub`; MCP servers are configured user-globally (out of this target's scope).
- All UPGRADE rows that touch hooks, policy, permissions, protected_files, CI, build/toolchain, or the vendor decision are **PROPOSE / owner-walled** — none weaken an existing guard; the only strength changes are additive (more denies, wider protection, more crates checked).
- No production code was read or modified; this audit is confined to the governance/settings/config plane.
