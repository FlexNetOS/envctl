# grit — governance/settings/config findings

axis: governance+settings+config
target: grit (/home/drdave/Desktop/meta/grit), pure-Rust CLI, version 0.4.0, edition 2021
default branch: master (origin/HEAD → master); two-branch model master←develop
meta membership: FlexNetOS/grit, tags [ai, rust, orchestration]
upstream note: `pr-head` remote = rtk-ai/grit; FlexNetOS/grit is a fork

## Surfaces scanned
| surface | path(s) | present | evidence |
|---|---|---|---|
| rules | `.claude/rules/*.md` | ABSENT | `.claude/` empty (find returned 0 files) |
| instructions: CLAUDE.md | `/grit/CLAUDE.md` | present | 160 lines, ICM block + RTK block only |
| instructions: AGENTS.md | `/grit/AGENTS.md` | present (stub) | 26 lines, ICM block ONLY |
| hooks: settings.json | `.claude/settings.json` | ABSENT | no `.claude/` content |
| hooks: handoff hooks.toml | `.handoff/hooks/hooks.toml` | ABSENT | no `.handoff/` dir |
| hooks: .githooks | `.githooks/*` | ABSENT | dir does not exist |
| hooks: preflight | `scripts/preflight.sh` | ABSENT | scripts/ has ai-agents,lib,sweep,synthetic,throughput only |
| policy | `.handoff/policies/rules.toml`, ADRs, PILOT.toml | ABSENT | no `.handoff/`, no `docs/adr-*.md` |
| runtime settings (claude/codex) | `.claude/settings.json`, `.codex/config.toml`, `.claude/agent-guard.toml` | ABSENT | `.codex/` empty; no agent-guard |
| runtime settings (rtk) | `.rtk/filters.toml` | present but UNTRUSTED | template only; rtk emits "Filters NOT applied. Run `rtk trust`" on every invocation |
| project/build config | `Cargo.toml`, `Cargo.lock` | present | Cargo.toml:1-66, lock 90.5K |
| toolchain pin | `rust-toolchain.toml`, `.cargo/config.toml`, `rustfmt.toml`, `clippy.toml`, `deny.toml` | ABSENT | none in repo root |
| CI | `.github/workflows/{ci,release,release-please,next-release,pr-target-check}.yml` | present | 5 workflows |
| release config | `release-please-config.json`, `.release-please-manifest.json` | present | config:1-10, manifest:1-3 |
| meta membership | `/meta/.meta.yaml` | present | .meta.yaml:197-200 |
| grit own config surface | `src/config.rs`, `src/db/{sqlite,s3,azure}_store.rs` | present | config.rs:1-127, store backends sqlite/s3/azure |
| MCP servers | `.codex/config.toml [mcp_servers.*]`, `.mcp.json` | ABSENT | no MCP surface in grit |
| skills | `.claude/skills/*`, `.agents/skills/*` | ABSENT | none |

## Hygiene detectors
| detector | result | evidence | risk |
|---|---|---|---|
| MCP rot | none found — grit declares zero MCP servers | no `.codex/config.toml`, no `.mcp.json` | none (library/CLI, not a harness host) |
| Skill overload | none found — zero skills, zero listing budget | no `.claude/skills/`, no `skillListingBudgetFraction` | none; but no harness wiring at all (see gov-001) |
| Token burn | RTK block (CLAUDE.md:28-160) advertises pnpm/npm/npx/vitest/tsc/next/prisma/docker/kubectl — all irrelevant to a single-crate Rust repo; ~130 lines of dead instruction surface burned every session | CLAUDE.md:92-128; repo has no package.json/bun.lock/node | low-med: instruction noise, no project-specific guidance to offset it |
| Token burn (dup) | ICM block duplicated verbatim in CLAUDE.md:1-26 and AGENTS.md:1-26 | identical 26-line block in both files | low: maintenance drift, double-read |
| Permission drift | NO destructive-command guard, NO PreToolUse deny, NO agent-guard.toml — agents in grit have unrestricted shell vs parent meta which gates `git reset --hard`/`rm -rf`/force-push | parent `/meta/.claude/rules/meta-destructive-commands.md` exists; grit has no equivalent local rule or hook | med: meta member with cloud-credential code and no destructive guard |
| Config drift (rtk) | rtk filters present but untrusted → token savings CLAUDE.md promises are NOT active here | rtk runtime warning on every command in this repo | low: advertised≠actual |
| Config drift (toolchain) | no MSRV (`rust-version`), no `rust-toolchain.toml`; CI floats on `dtolnay/rust-toolchain@stable` | Cargo.toml has no rust-version; ci.yml:32 / release.yml:52 use `@stable` | med: non-reproducible builds; a future stable can break master + auto-merge |
| Config drift (supply-chain) | no `cargo audit` / `cargo deny` / `deny.toml` in CI despite aws-sdk-s3, azure_* SDKs and cloud-credential handling | grep of `.github/workflows/` → "no supply-chain gate"; Cargo.toml:52-60 | med: CVE blind spot in a credential-handling tool |
| Config drift (release org) | release/next-release/pr-target workflows hardcode `rtk-ai/grit` and `rtk-ai/homebrew-tap`; meta member repo is `FlexNetOS/grit` | release.yml:148,167,173,214; next-release.yml:29 `ALLOWED_REPOS: "rtk-ai/grit"` | med: release/homebrew jobs target wrong org when run from FlexNetOS fork |
| Config drift (meta) | grit tags `[ai, rust, orchestration]`; meta audit history flagged grit `[untriaged]`/MISSING classification earlier | .meta.yaml:200; /meta/.meta/audit-findings.txt:99,188 | low: now triaged, residual policy nit |

## CLAIM rows
- CLAIM[gov-001] axis: governance+settings+config | surface: `.claude/`, `.handoff/`, `.codex/` | evidence: `find .claude/.codex/.handoff` → 0 files | confidence: High | grit has NO agent control plane at all — no rules, hooks, policy, settings.json, or agent-guard — despite being a registered meta member tagged `orchestration` and shipping cloud-credential code paths. A missing expected surface is a finding, not a pass.
- CLAIM[gov-002] axis: governance+settings+config | surface: `/grit/AGENTS.md` | evidence: AGENTS.md:1-26 (entire file) | confidence: High | AGENTS.md is a pure ICM stub: no mission, no hard rules, no fail-closed law, no destructive-command guard, no pointer to the develop-only PR rule or RELEASE_FLOW.md. The parent-meta AGENTS.md convention (mission + hard rules + fail-closed) is absent.
- CLAIM[gov-003] axis: governance+settings+config | surface: `/grit/CLAUDE.md` | evidence: CLAUDE.md:28-160 | confidence: High | CLAUDE.md carries no harness pointer and no change-history table; its body is the generic RTK block listing pnpm/npm/vitest/tsc/next/prisma/docker/kubectl — none applicable to this single-crate Rust repo. Cross-surface: a CLAUDE.md "Harness:" pointer (present in parent meta) has no counterpart here.
- CLAIM[gov-004] axis: governance+settings+config | surface: `.rtk/filters.toml` | evidence: rtk runtime: "untrusted project filters (.rtk/filters.toml) — Filters NOT applied. Run `rtk trust`" | confidence: High | The committed rtk filter file is untrusted, so the token-optimization tooling CLAUDE.md mandates is silently inactive in this repo: advertised behavior ≠ actual behavior.
- CLAIM[gov-005] axis: governance+settings+config | surface: `Cargo.toml` + CI | evidence: Cargo.toml has no `rust-version`; ci.yml:32 + release.yml:52 `dtolnay/rust-toolchain@stable`; no `rust-toolchain.toml` in repo | confidence: High | No MSRV and no toolchain pin: every CI run floats on whatever `stable` is current; a toolchain bump can break master with no declared floor, and there is no auto-merge preflight hook installed here to catch it locally.
- CLAIM[gov-006] axis: governance+settings+config | surface: `.github/workflows/ci.yml` | evidence: ci.yml:53 `cargo clippy -- -D warnings` | confidence: High | grit's clippy gate omits `--all-targets`, so lints in test code (`#[cfg(test)]`) are NOT gated by CI. For a repo whose suite (config.rs:59-127, db stores) is test-heavy, test-code lint rot can accumulate undetected.
- CLAIM[gov-007] axis: governance+settings+config | surface: CI | evidence: grep `.github/workflows/` for audit/deny → none; Cargo.toml:52-60 aws-config, aws-sdk-s3, azure_core/azure_storage/azure_storage_blobs | confidence: High | No `cargo audit`/`cargo deny` gate exists despite a large cloud-SDK dependency surface and code that handles cloud access keys (azure_store.rs:29). Advisories on these deps reach master ungated.
- CLAIM[gov-008] axis: governance+settings+config | surface: `src/config.rs` + `src/cli/mod.rs` | evidence: config.rs:11 `backend: String`; cli/mod.rs:392 `match config.backend.as_str()` with cli/mod.rs:407 `_ => SqliteLockStore` | confidence: High | The backend selector is a loose string with a silent catch-all: a typo (`"azur"`, `"s33"`) does not error — it silently routes locks to the local SQLite store. A misconfigured shared-backend repo would degrade to per-machine local locking with no signal, defeating grit's coordination purpose.
- CLAIM[gov-009] axis: governance+settings+config | surface: `src/db/azure_store.rs` + `src/config.rs` | evidence: azure_store.rs:29 `pub access_key: String` (serde Serialize, no skip); config.rs:51-56 `save()` uses `std::fs::write` with no mode restriction | confidence: High | Azure access keys are written in plaintext to `.grit/config.json` with default (group/world-readable) permissions. .gitignore:3 (`.grit`) prevents commit, but the at-rest secret is unprotected on multi-user hosts and the S3 path mirrors this shape (s3_store.rs S3Config). No `set-azure`/`set-s3` redaction or 0600 chmod.
- CLAIM[gov-010] axis: governance+settings+config | surface: `.github/workflows/release.yml`, `next-release.yml` | evidence: release.yml:148 `--repo rtk-ai/grit`, :167/:173/:184 rtk-ai URLs, :214 `repos/rtk-ai/homebrew-tap`; next-release.yml:29 `ALLOWED_REPOS: "rtk-ai/grit"` | confidence: Medium | Release/homebrew/next-release workflows hardcode the upstream `rtk-ai` org. The meta member and `origin` are `FlexNetOS/grit` (a fork with a separate `pr-head=rtk-ai/grit` remote). If release-please ever fires on the FlexNetOS fork, the homebrew/checksum-download jobs target the wrong org and fail (or require `HOMEBREW_TAP_TOKEN` for rtk-ai).
- CLAIM[gov-011] axis: governance+settings+config | surface: cross-surface (parent meta ↔ grit) | evidence: `/meta/.claude/rules/meta-destructive-commands.md` exists; grit has no local rule/hook and CLAUDE.md/AGENTS.md never mention destructive-command safety | confidence: High | Parent-meta destructive-command governance (blocks `git reset --hard`, `rm -rf`, force-push via PreToolUse) is not propagated into grit. An agent invoked with grit as cwd inherits no guard — a fail-OPEN drift across the governance boundary.

## UPGRADE rows
- UPGRADE[gov-001] axis:accuracy | target_surface: `/grit/AGENTS.md` + new `.claude/rules/` | risk_tier: PROPOSE | rationale: AGENTS.md stub (gov-002) and absent destructive guard (gov-011) leave a credential-handling meta member with zero hard rules; this is owner-canon. blast-radius: every agent session in grit. acceptance: AGENTS.md contains a Mission, Hard Rules, and Fail-closed sections AND a `.claude/rules/destructive-commands.md` mirroring the parent-meta guard exists; grep both for "develop" PR rule + "reset --hard". reversibility: high (docs/rules, revertible). evidence: AGENTS.md:1-26, /meta/.claude/rules/meta-destructive-commands.md. NEVER weaken — this only ADDS guards.
- UPGRADE[gov-002] axis:quality | target_surface: `/grit/CLAUDE.md` | risk_tier: APPLY | rationale: gov-003/gov-001(dup ICM) — strip the irrelevant pnpm/npm/vitest/tsc/next/docker RTK lines, keep Rust-relevant rtk + ICM once, add a harness/change-history pointer; non-executable docs. blast-radius: instruction surface only. acceptance: CLAUDE.md no longer references pnpm/vitest/tsc/next AND ICM block appears once across CLAUDE.md+AGENTS.md. reversibility: high. evidence: CLAUDE.md:92-128, AGENTS.md:1-26.
- UPGRADE[gov-003] axis:accuracy | target_surface: `.rtk/filters.toml` trust state | risk_tier: PROPOSE | rationale: gov-004 — committed rtk filters are inert until trusted; either trust them (owner action: `rtk trust`) or remove the dead template so CLAUDE.md's RTK promise matches reality. blast-radius: token-savings tooling in this repo. acceptance: `rtk` no longer emits "Filters NOT applied" in grit, OR `.rtk/filters.toml` removed and CLAUDE.md RTK section trimmed. reversibility: high. evidence: rtk runtime warning; `.rtk/filters.toml`.
- UPGRADE[gov-004] axis:accuracy | target_surface: `Cargo.toml` + `rust-toolchain.toml` | risk_tier: PROPOSE | rationale: gov-005 — declare `rust-version` (MSRV) in Cargo.toml and pin a `rust-toolchain.toml`/channel so CI is reproducible and master can't be broken by a floating `stable`. blast-radius: build/CI for the whole crate. acceptance: `cargo +<pinned> build` succeeds AND Cargo.toml has `rust-version`; CI references the pin. reversibility: high (additive config). evidence: Cargo.toml (no rust-version), ci.yml:32, release.yml:52.
- UPGRADE[gov-005] axis:accuracy | target_surface: `.github/workflows/ci.yml` | risk_tier: PROPOSE | rationale: gov-006/gov-007 — make clippy `cargo clippy --all-targets -- -D warnings` and add a `cargo audit`/`cargo deny` job to gate test-code lints and dep advisories on a credential-handling tool. Tightening a gate (never weaken). blast-radius: CI gate strictness. acceptance: CI fails on an injected test-code clippy lint AND on a known-advisory dep; `deny.toml` present. reversibility: high. evidence: ci.yml:53, Cargo.toml:52-60.
- UPGRADE[gov-006] axis:accuracy | target_surface: `src/config.rs` + `src/cli/mod.rs` | risk_tier: PROPOSE | rationale: gov-008 — replace `backend: String` + catch-all `_ => sqlite` with a `#[derive(Deserialize)] enum Backend { Local, S3, Azure }` (deny-unknown) so a typo'd backend is a hard config error, not a silent downgrade to local locking. blast-radius: config parse + resolve_lock_store (cli/mod.rs:388-412); deny-unknown changes load() behavior on legacy files — needs a fail-closed-with-clear-error path. acceptance: loading `{"backend":"azur"}` returns an error naming valid backends instead of opening SQLite (add a RED test alongside config.rs:95-103). reversibility: medium (touches parse contract). evidence: config.rs:11, cli/mod.rs:392-411.
- UPGRADE[gov-007] axis:accuracy | target_surface: `src/config.rs` `save()` + secret fields | risk_tier: PROPOSE | rationale: gov-009 — write `.grit/config.json` with 0600 perms and/or move access keys out of the serialized config (env/keyring reference), since plaintext credentials at default perms are an at-rest exposure. Security-hardening, owner-walled. blast-radius: config save/load + set-azure/set-s3 paths. acceptance: post-`set-azure`, `.grit/config.json` mode is 0600 AND/OR access_key is not stored verbatim; existing roundtrip tests still pass. reversibility: medium. evidence: config.rs:51-56, azure_store.rs:29.
- UPGRADE[gov-008] axis:accuracy | target_surface: `.github/workflows/release.yml`, `next-release.yml` | risk_tier: PROPOSE | rationale: gov-010 — parameterize the hardcoded `rtk-ai/grit`/`rtk-ai/homebrew-tap` to `${{ github.repository }}` (and a configurable tap) so the fork's release path doesn't silently target upstream. blast-radius: release/homebrew automation. acceptance: a dry-run release from FlexNetOS/grit resolves all `--repo`/URL refs to FlexNetOS, not rtk-ai. reversibility: high. evidence: release.yml:148/167/214, next-release.yml:29.

## Gaps / owner walls
- Owner-wall: whether grit SHOULD carry a full `.handoff`/`.claude` harness, or is intentionally a thin library/CLI member. gov-001/gov-002/gov-011 assume a meta member should at minimum mirror the destructive-command guard; confirm scope before adding a harness.
- Owner-wall: rtk filter trust (gov-003) and the rtk-ai-vs-FlexNetOS fork relationship (gov-010) are owner decisions — is FlexNetOS/grit meant to publish releases at all, or only consume from upstream rtk-ai?
- Inconclusive: S3 store credential storage shape was inferred from the AzureConfig pattern and config.rs serialization; s3_store.rs S3Config (s3_store.rs:636) was not line-verified for an embedded secret field — verify before acting on the S3 half of gov-009 (Azure half is confirmed).
- Not assessed: `.handoff/loop/plan/loop_state.md` and prior plan-cartographer graph artifacts were not present/consulted for grit; this scan is governance/settings/config-only.
