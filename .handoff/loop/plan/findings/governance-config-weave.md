# weave — governance/settings/config findings

Axis: **governance+settings+config**. Target: **weave** (A2A transport plane; cycle 4).
Code (READ-ONLY): `/home/drdave/Desktop/meta/.worktrees/plan-weave-red/weave` @ snapshot `4fe2419` (codemap).
Context read: `reports/codemap-weave.md`, `research/weave.trends.md`.
Method: read-only; fail-closed; every CLAIM cites file:line. Branch-protection required-checks and the `hf` binary
internals live outside this worktree → marked owner-wall, not silently passed.

## Surfaces scanned
| surface | path(s) | present | evidence |
|---|---|---|---|
| instructions (operating contract) | `CLAUDE.md` (16.1KB) | yes | `CLAUDE.md:5-9,20-26,28-46,47-59,112-120` — harness pointer, Rust-native invariant, branch model, gate |
| instructions (root AGENTS.md) | `AGENTS.md` | **no** | absent; `.codex/AGENTS.md:3` references a "root `AGENTS.md`" that does not exist (dangling pointer) |
| instructions (codex baseline) | `.codex/AGENTS.md` | yes | `.codex/AGENTS.md:1-26` — ECC supplement, MCP baseline, multi-agent roster |
| instructions (ECC sidecar skill) | `.agents/skills/weave/SKILL.md` | yes | self-corrected after misinformation drift (`SKILL.md:9` "earlier auto-generated version was inaccurate") |
| rules | `.claude/rules/*.md` | **no** (none in repo) | weave carries no repo-local `.claude/rules/`; governance lives in CLAUDE.md + skills + .handoff |
| runtime settings | `.claude/settings.json`, `settings.local.json` | **no** (by design) | weave WRITES hooks into the *consumer's* settings.json via `weave setup` (`main.rs:92`,`setup.rs`); no repo-committed settings.json |
| agents (Claude) | `.claude/agents/*.md` (5) | yes | `weave-planner/implementer/verifier/guardian` + `continuity-steward` |
| skills (Claude) | `.claude/skills/*/SKILL.md` (6) | yes | `weave-orchestrator,weave-invariants,weave-test-discipline,weave-drift-guard,weave-loop,session-relay` |
| commands | `.claude/commands/*.md` (2) | yes | `session-relay.md`, `weave-loop.md` |
| ECC tool config | `.claude/ecc-tools.json`, `.claude/identity.json` | yes | `ecc-tools.json` v1.3, generated 2026-06-02; `referenceSetReadiness.score=0/7` (inert) |
| instincts (generated) | `.claude/homunculus/instincts/inherited/weave-instincts.yaml` | yes | inherited ECC instincts (5.0KB sidecar) |
| codex agents | `.codex/agents/*.toml` (3) + `.codex/{explorer,reviewer,docs-researcher}.toml` | yes | `.codex/config.toml:38-47` |
| hooks (kernel contract) | `.handoff/hooks/hooks.toml` | yes | `handoff.hooks.v1`, **8** typed events declared (`hooks.toml:9-61`) |
| hooks (kernel scripts) | `.handoff/hooks/{loop-entry.sh,session-end.sh}` | **no** | only `hooks.toml`; no shell scripts (events run `hf` verbs directly) |
| hooks (weave's own runtime) | `weave hook <event>` + PreToolUse gate | yes | `main.rs:27,861-862,8782-8785,8793-8922` — session/prompt/stop/wake/notification/pretooluse |
| policy (loop) | `.handoff/policy.toml` | yes | `handoff.policy.v1`; remote/loop/merge/preflight/sync/ci blocks |
| policy (rules) | `.handoff/policies/rules.toml` | yes | `handoff.policy.rules.v1`; fail-closed defaults, protected_files, blocked commands |
| ADRs | `.handoff/decisions/ADR-0001..0005*.md` | yes | adopt-kernel, obscura, token-light, rust-native, cross-machine-push |
| meta registration | `../.meta.yaml` | yes | `.meta.yaml:178-181` — `weave` tags `[mcp, orchestration]`, repo `FlexNetOS/weave` |
| project/build | `Cargo.toml`, `Cargo.lock`, `rustfmt.toml`, `deny.toml` | yes | workspace v0.2.0, 4 members; `deny.toml` advisory gate |
| toolchain pin | `rust-toolchain.toml`, `.cargo/config.toml` | **no** | no pinned toolchain/MSRV; CI floats on `dtolnay/rust-toolchain@stable` |
| CI | `.github/workflows/{ci.yml,sync-master.yml}` | yes | `ci.yml` 10 jobs; `sync-master.yml` 7 required-check gate |
| bun/node/pnpm config | `package.json`, `bun.lock`, `bunfig.toml` | N/A — Rust-native repo, no JS toolchain in build (Node only via `.codex` MCP `npx`) |

## Hygiene detectors
| detector | result | evidence | risk |
|---|---|---|---|
| MCP rot | **6 npx (Node) servers, none verified used by weave's Rust harness** | `.codex/config.toml:8-29` github/context7/exa/memory/playwright/sequential-thinking — all `npx -y`; `memory` overlaps ICM; require a Node runtime in a no-Node/no-Python repo | Medium (token/runtime burn, unused-server rot) |
| Skill overload | **none found — 6 lean skills (5.4–8.9KB), all weave-specific** | `.claude/skills/*` sizes 5464–8878B; ECC `weave` skill already removed from `.claude/skills/` for misinformation drift (`CLAUDE.md:15`) | Low |
| Token burn (MCP standing surface) | **actively guarded — POSITIVE** | `CLAUDE.md:119` `MAX_STANDING_TOOLS_BYTES` (~2k tokens), single `weave` meta-tool via progressive disclosure; budget test `standing_mcp_surface_is_within_token_budget` | Low |
| Token burn (codex agents) | bounded: `max_threads=6, max_depth=1` | `.codex/config.toml:34-36` | Low |
| Permission drift | **PreToolUse gate has real teeth but is OPT-IN/off-by-default; merge permission_gate is "transitional"** | `main.rs:8900-8913` deny-by-default; `policy.toml:23` `permission_gate=true # transitional → AI gatekeeper` | Medium |
| Config drift (CI gate count) | **docs undercount the gate: "six" documented vs "seven" enforced (audit omitted)** | `CLAUDE.md:43` & `policy.toml:37` = 6; `sync-master.yml:46` = 7 incl. `audit` | High |
| Config drift (language) | **Python in CI vs "no Python" Rust-native invariant** | `ci.yml:69,181` `python3 scripts/{target_smoke,supply_chain_audit}.py`; `CLAUDE.md:22,47-59` | High |
| Config drift (generated repo URL) | **ecc-tools.json names wrong repo owner** | `ecc-tools.json` repo `drdave-flexnetos/weave` vs canonical `FlexNetOS/weave` (`policy.toml:10`, `.meta.yaml:179`) | Low |
| Config drift (toolchain pin) | no `rust-toolchain.toml`/MSRV; CI floats on `@stable` | `ci.yml:27,39,...`; no pin file | Low-Medium |
| Advisory/supply-chain gate | **scoped, documented, time-bounded, CI-gated — POSITIVE** | `deny.toml:29-44` 5 RUSTSEC ids, all `libsql` remote-TLS, upstream-blocked (WL-044b); `weave.trends.md:154-198` confirms default build advisory-clean | Low (monitor each cycle) |
| Protected-file guard | **strong — POSITIVE** | `rules.toml:37-50` blocks PRs touching `.github/**`, `.handoff/policy.toml`, policies/hooks/decisions, `CLAUDE.md`, `**/Cargo.toml`, `**/*.lock` | Low |
| Destructive-command guard | present | `rules.toml:52-62` blocks `rm -rf /`, `git reset --hard`, `git clean -fdx`, `git push --force`, `curl|sh`, etc. | Low |

## CLAIM rows
- CLAIM[gov-001] axis: governance+settings+config | surface: `weave/src/main.rs:8793-8922` (+ `CLAUDE.md:201-206`) | evidence: PreToolUse gate is **real teeth, not a fail-OPEN seam** — for a dangerous tool it is DENY-BY-DEFAULT (`main.rs:8900-8913` denies when no approver / broadcast approver), enforces its OWN short timeout and emits explicit `deny` rather than relying on Claude's 600s fail-open timeout (`main.rs:8805-8809`), and confirms danger in-drain via `pretooluse_is_dangerous` so an over-broad matcher can't sneak through (`main.rs:8812-8828`). This is the contrast to handoff/rusty-idd's fail-OPEN seam. **Caveat:** it is OPT-IN — installed only by `weave setup --pretooluse` AND inert unless `pretooluse_approver`/`WEAVE_PRETOOLUSE_APPROVER` is set (without it, fail-closed = deny everything). | confidence: High
- CLAIM[gov-002] axis: governance+settings+config | surface: `CLAUDE.md:5-9` vs `.claude/skills/` + `.claude/agents/` | evidence: the "## Harness:" pointer names only `weave-orchestrator,weave-invariants,weave-test-discipline,weave-drift-guard` skills and "agents in `.claude/agents/weave-*.md`", but two real skills (`weave-loop`, `session-relay`) and one real agent (`continuity-steward.md`, which the `weave-*.md` glob excludes) have **no pointer** — pointer↔reality drift (skills/agent present with no entry in the operating contract). | confidence: High
- CLAIM[gov-003] axis: governance+settings+config | surface: `CLAUDE.md:43` + `.handoff/policy.toml:37` vs `.github/workflows/sync-master.yml:46` | evidence: the operating contract and machine policy both document **six** required checks (`rustfmt, clippy, test, build (libsql backend), sign, libsql + sign`), but the actual promotion gate enforces **seven** — adding `audit` (the WL-044 supply-chain `cargo deny` gate). The teeth-bearing supply-chain gate is enforced yet absent from both human-facing and machine policy lists → understated gate. | confidence: High
- CLAIM[gov-004] axis: governance+settings+config | surface: `.github/workflows/ci.yml:69,181` (+ `scripts/{target_smoke.py,supply_chain_audit.py}`) | evidence: CI invokes `python3 scripts/target_smoke.py` (line 69, target-smoke job) and `python3 scripts/supply_chain_audit.py` (line 181, audit job) — Python wired into the CI gate of a repo whose stated core invariant is "No Python" (`CLAUDE.md:22`) and whose CRITICAL section guards against any non-Rust build/CI intrusion (`CLAUDE.md:47-59`). `target_smoke.py` is 24KB / `supply_chain_audit.py` 6.5KB of build-plane Python = language drift in the tooling plane. | confidence: High
- CLAIM[gov-005] axis: governance+settings+config | surface: `.handoff/policy.toml:6` vs `CLAUDE.md:42` | evidence: the policy.toml header prose says "PRs target the protected master trunk", directly contradicting CLAUDE.md's corrected model "Open **every PR into `develop`** — never into `master` directly" (`CLAUDE.md:42`, history `CLAUDE.md:18` records the reversal was already corrected). The policy *keys* (`base_branch="develop"`, `trunk_branch="master"`, `develop_mirrors_trunk=true`, `policy.toml:11-13`) match CLAUDE.md; only the stale comment carries the old reversed model — drift inside a protected governance file. | confidence: High
- CLAIM[gov-006] axis: governance+settings+config | surface: `.codex/config.toml:8-29` (+ `.codex/AGENTS.md:13-14`) | evidence: ECC-generated baseline declares six MCP servers (`github, context7, exa, memory, playwright, sequential-thinking`) all launched via `npx -y` (Node). They are an "ECC-safe baseline" for Codex CLI work, none verified consumed by weave's Rust harness; `memory` (server-memory) duplicates ICM, `context7`+`exa` overlap, and `playwright` pulls a browser stack — all requiring a Node runtime in a no-Node/no-Python Rust-native repo = MCP rot + runtime/token burn risk. | confidence: Medium
- CLAIM[gov-007] axis: governance+settings+config | surface: `.claude/ecc-tools.json` (repo field) vs `.handoff/policy.toml:10` + `.meta.yaml:179` | evidence: ecc-tools.json records repo `https://github.com/drdave-flexnetos/weave`, but the canonical origin is `FlexNetOS/weave` (`policy.toml:10`) / `git@github.com:FlexNetOS/weave.git` (`.meta.yaml:179`). Generated sidecar carries a stale/wrong owner; also `referenceSetReadiness.score=0/7` (inert). | confidence: Medium
- CLAIM[gov-008] axis: governance+settings+config | surface: `.codex/AGENTS.md:3` | evidence: states "This supplements the root `AGENTS.md`" but no root `AGENTS.md` exists in the repo — dangling instruction pointer. | confidence: High
- CLAIM[gov-009] axis: governance+settings+config | surface: `.handoff/hooks/hooks.toml:11-61` | evidence: the kernel hook contract declares **8** typed events (SessionStart, TaskClaim, PreEdit, PostEdit, PreHandoff, SessionStop, PreSessionStart, PostMerge) of the kernel's 14-event vocabulary; several **`fail_mode="block"`** hooks invoke `hf` verbs explicitly tracked as **unimplemented** (`hf policy check-claim` HFTASK-0015, `hf policy check-edit` HFTASK-0015, `hf drift`+`check-handoff` HFTASK-0005/0015, `hf session preflight` HFTASK-0007). Whether an unimplemented verb fails closed (good) or no-ops (fail-OPEN) depends on the shared `hf` binary, which is out of this worktree. | confidence: Medium
- CLAIM[gov-010] axis: governance+settings+config | surface: `.handoff/policy.toml:20-24` | evidence: the merge gate sets `require_review=true`, `require_permission_granted=true`, but annotates `permission_gate=true # transitional → AI gatekeeper` and reviewer `cloud_ultra # Phase 2 = "swarm_local"` — the permission gate is declared-but-transitional, so its enforcement is aspirational rather than wired. | confidence: Medium
- CLAIM[gov-011] axis: governance+settings+config | surface: repo root (no `rust-toolchain.toml`/`.cargo/config.toml`) vs `.github/workflows/ci.yml:27,39,51` | evidence: no pinned toolchain or MSRV; every CI job uses floating `dtolnay/rust-toolchain@stable`, so a future stable rustc/clippy can change `-D warnings` behavior and break the gate without a code change — build-reproducibility config gap. | confidence: High
- CLAIM[gov-012] axis: governance+settings+config | surface: `deny.toml:29-44` + `weave.trends.md:154-198` | evidence: the advisory ignore-list (5 RUSTSEC ids: 2026-0098/0099/0049/0104 rustls-webpki + 2025-0134 rustls-pemfile) is a standing relaxation, but it is scoped to the optional `libsql` remote-TLS feature (default build matches none), each id documented with rationale + removal trigger (WL-044b), `[graph] all-features=true` so the scan has teeth, and any *unlisted* advisory fails CI — POSITIVE governance. The relaxation is upstream-blocked on libsql adopting rustls 0.23 and must be re-audited each cycle (`weave.trends.md:172-178`). | confidence: High

## UPGRADE rows
- UPGRADE[gov-001] axis: governance+settings+config | target_surface: `CLAUDE.md:43` + `.handoff/policy.toml:36-37` | impact: aligns the documented gate with reality so the WL-044 supply-chain `audit` check is a first-class, discoverable gate (closes a silent under-statement of the merge gate) | effort: S | risk_tier: PROPOSE (both files are owner-canon in `rules.toml:40-50` protected_files) | acceptance: `policy.toml [ci] required_checks` and CLAUDE.md's check enumeration each list all 7 names that `sync-master.yml:46` enforces (`rustfmt, clippy, test, build (libsql backend), sign, libsql + sign, audit`); a diff of the three lists is empty | reversibility: trivial (revert the doc edit) | evidence: CLAIM[gov-003]
- UPGRADE[gov-002] axis: governance+settings+config | target_surface: `scripts/target_smoke.py`, `scripts/supply_chain_audit.py` + `.github/workflows/ci.yml:69,181` | impact: removes the last Python from the build/CI plane, restoring the Rust-native invariant (`CLAUDE.md:47-59`); a Rust binary/`xtask` smoke + a `cargo deny`-driven posture check replace 30KB of Python | effort: M | risk_tier: PROPOSE (CI + build-tooling; never weaken — must reproduce the same gate outputs) | acceptance: `find . -name '*.py' -not -path '*/target/*'` returns empty AND CI's `target artifact smoke` + `audit` jobs pass with Rust-native steps producing the same `target-smoke.json` schema | reversibility: medium (git revert restores the .py scripts + CI lines) | evidence: CLAIM[gov-004]
- UPGRADE[gov-003] axis: governance+settings+config | target_surface: `CLAUDE.md:5-9` (Harness pointer) | impact: the operating contract enumerates every shipped skill/agent so an agent can discover `weave-loop`, `session-relay`, and the `continuity-steward` agent (closes pointer↔reality drift) | effort: S | risk_tier: PROPOSE (CLAUDE.md is protected canon) | acceptance: every `.claude/skills/*` dir and every `.claude/agents/*.md` is named (or globbed by a corrected pattern) in CLAUDE.md's Harness section; `comm -23` of {real} vs {pointed} is empty | reversibility: trivial | evidence: CLAIM[gov-002]
- UPGRADE[gov-004] axis: governance+settings+config | target_surface: `.handoff/policy.toml:6` (header comment) | impact: removes the reversed branch-model prose so the protected policy file cannot mislead a future agent into PRing against `master` | effort: S | risk_tier: PROPOSE (protected file) | acceptance: the policy.toml comment states PRs target `develop`, matching `CLAUDE.md:42`; no occurrence of "PRs target the protected master trunk" remains | reversibility: trivial | evidence: CLAIM[gov-005]
- UPGRADE[gov-005] axis: governance+settings+config | target_surface: `.codex/config.toml:8-29` | impact: prune MCP servers to those actually used (drop dead/duplicate `memory`/`playwright`/`sequential-thinking` if unused; resolve `context7`/`exa` overlap), cutting Node-runtime and token surface in a Rust-native repo | effort: S | risk_tier: PROPOSE (MCP/settings change; owner decides which servers the Codex lane needs) | acceptance: each remaining `[mcp_servers.*]` has a documented consumer in the weave/Codex workflow; removed servers have no referencing skill/agent | reversibility: trivial (re-add the block) | evidence: CLAIM[gov-006]
- UPGRADE[gov-006] axis: governance+settings+config | target_surface: `.claude/ecc-tools.json` (repo field) | impact: generated metadata names the correct origin `FlexNetOS/weave` | effort: S | risk_tier: REGENERATE (auto-generated by `ecc-tools` — re-run the owning tool against the correct remote rather than hand-edit) | acceptance: `ecc-tools.json` repo field == `FlexNetOS/weave` (matching `.meta.yaml:179`); regenerated by the tool, not by hand | reversibility: trivial (regenerate again) | evidence: CLAIM[gov-007]
- UPGRADE[gov-007] axis: governance+settings+config | target_surface: `.codex/AGENTS.md:3` | impact: removes the dangling "root `AGENTS.md`" reference (either drop the clause or add a real root AGENTS.md) so the codex baseline doesn't point at a non-existent file | effort: S | risk_tier: APPLY (non-protected generated doc; pure wording) | acceptance: `.codex/AGENTS.md` contains no reference to a missing root AGENTS.md, OR a root `AGENTS.md` exists | reversibility: trivial | evidence: CLAIM[gov-008]
- UPGRADE[gov-008] axis: governance+settings+config | target_surface: new `rust-toolchain.toml` at repo root | impact: pins the toolchain channel (and optional MSRV) so a future `@stable` bump can't silently change `-D warnings`/clippy behavior and false-break the gate; reproducible builds across the meta workspace | effort: S | risk_tier: PROPOSE (toolchain config — affects every build/CI job) | acceptance: `rust-toolchain.toml` exists with a pinned `channel`; CI jobs resolve it instead of bare `@stable`; a clean `cargo build/clippy/test` passes on the pinned channel | reversibility: trivial (delete the file → revert to floating stable) | evidence: CLAIM[gov-011]
- UPGRADE[gov-009] axis: governance+settings+config | target_surface: `weave setup` default + `CLAUDE.md:201-206` | impact: document (and optionally default-arm with a configured approver) the PreToolUse gate so weave's strongest control isn't a dormant opt-in — a security TIGHTENING, never a relaxation | effort: M | risk_tier: PROPOSE (changes install/default behavior of a security gate; owner-walled) | acceptance: `weave setup` clearly surfaces the gate + approver requirement, and CLAUDE.md documents the off-by-default state and how to arm it; no path weakens the deny-by-default semantics in `main.rs:8900-8913` | reversibility: high (revert to current opt-in default) | evidence: CLAIM[gov-001]

## Gaps / owner walls
- **Branch-protection required-checks (owner wall):** the *true* enforced required-check set lives in GitHub branch protection, not in-repo. `sync-master.yml:46` requires 7 names, but whether the PR-merge gate (`gh pr merge --auto`) also requires all 7 — incl. `audit`, `surfaces`, `libsql + surfaces`, `target-smoke` — cannot be confirmed from the worktree. If `audit`/`surfaces` are NOT branch-protected required checks, the supply-chain and human-surface gates are fail-OPEN on merge despite running in CI. Needs `gh api repos/FlexNetOS/weave/branches/{master,develop}/protection`.
- **`hf` verb implementation (owner wall):** CLAIM[gov-009] block-mode hooks call `hf` verbs tagged unimplemented (HFTASK-0005/0007/0015). Whether they fail closed or no-op (fail-OPEN) depends on the shared `hf` binary in `meta/handoff`, outside this worktree. Inconclusive here.
- **`.claude/settings.json` cross-check (N/A — by design):** weave ships no repo settings.json; its lifecycle hooks + PreToolUse gate are merged into the *consumer's* host settings.json by `weave setup`. The hooks.toml↔settings.json↔*.sh tri-surface contract cannot be cross-checked in-repo because two of the three planes are deployed into the host, not committed here.
- **`.handoff/hooks/{loop-entry.sh,session-end.sh}` absent (informational):** the kernel's expected shell hook scripts are not present; `hooks.toml` runs `hf` verbs directly instead. Not a defect on its own, but means there is no in-repo shell layer to audit for the kernel events.
- **Python script behavior parity (inconclusive):** CLAIM[gov-004] flags Python in CI as language drift; whether `target_smoke.py`/`supply_chain_audit.py` encode logic that must be preserved on a Rust port is a porting-scope question for feature-forge, not resolvable here.
