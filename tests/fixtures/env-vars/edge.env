# comment rows are ignored
PLAIN=value
export EXPORTED=two
QUOTED_SINGLE='hello # not comment'
QUOTED_DOUBLE="hello world"
INLINE=abc # stripped comment
MULTI=line1\
line2
DUP=first
DUP=second
SECRET_TOKEN=super-secret
EXPAND=$HOME/bin:${PATH}
UNSAFE=$(whoami)
EMPTY=
INVALID LINE
