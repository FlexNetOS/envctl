# Harness lessons ledger (Feature Forge + ejected harnesses)

Append-only across runs. Recurrence is the point — never truncate. The evolution-steward appends
one row per mined lesson at every run boundary (DONE / HAND OFF), per the `harness-evolution`
method. Row format:

```
| date | harness | lesson (class, generalized) | evidence (cycle/finding) | recurrence | routed-to | status |
```

| date | harness | lesson (class, generalized) | evidence | recurrence | routed-to | status |
|------|---------|------------------------------|----------|------------|-----------|--------|
| 2026-06-17 | feature-forge | Verify a triggering claim that asserts concrete code state against HEAD before designing — cross-session relay/handoff claims go stale; a plan built on a false premise wastes a cycle (no-fabricate applied to inputs). | G2 #116 asserted `inject.rs`/`run_child = todo!()`, false at HEAD; orchestrator verified first (Friction-low note) | 1 | skills/feature-forge (Phase 0 step 4) | applied |
| 2026-06-17 | feature-forge | A stated gap usually implies an adjacent unstated gap — the architect must trace the full call path and fold any missing seam (missing field/hardcoded branch/unreachable variant) into the plan, not discover it at build time. | G2: request named native minting but `MintReq` lacked `mode` ⇒ `NativeSubtoken` unreachable via `Mint`; architect folded fix into U4 (01_architect_plan.md U4) | 1 | agents/feature-architect (working principles) | applied |
| 2026-06-17 | feature-forge | Sync-engine→async-daemon I/O has a fixed envctl idiom (off-reactor `block_on` via captured `Handle` inside `spawn_blocking`; reuse the frozen `build_upstream_client`, add no dep; key-free error strings) — capture it so it isn't re-invented per feature. | G2 R1 (load-bearing), implementer `DaemonHttpTransport` (02_implementer_log.md; transport.rs) | 1 | skills/rust-feature-impl (new "daemon seam idiom" section) | applied |
| 2026-06-17 | feature-forge | The guardian must classify clippy findings by BOTH axis (gate form vs `--all-targets`) and origin (touched vs untouched file): `--all-targets`-only in an untouched file = inherited red (NOTE, with `git diff` proof, never silently "fixed"); same axis in a touched file = a real finding to fix. Never relax the gate to clear red. | G2: `gui/main.rs:1997` fired only under `--all-targets` in an untouched file → correctly NOTE not blocker (03_guardian_report.md Finding #1) | 1 | skills/rust-feature-impl/references/verification.md (§0) | applied |
| 2026-06-17 | feature-forge | Phase-1.5 routing counts *independent* modules, not raw modules — a strict dependency chain (parallelism 0) stays sequential even when n>3; honor the architect's explicit routing recommendation over the raw count. | G2: 6 modules but strict U1→U6 chain ⇒ correctly sequential (Phase 1.5; 01_architect_plan.md `## Target repos`) | 1 | skills/feature-forge (Phase 1.5) | applied |
| 2026-06-18 | forge-loop | "Merged" ≠ "clean": workspace hygiene is a first-class loop OUTPUT, not a side effect. A loop that spawns a per-cycle resource (worktree/branch) MUST reap it at a settled boundary, or it accumulates unboundedly while every PR shows green. | 46 worktrees / 85 local branches / 17 stale remotes accrued over 18 cycles though all code was merged; owner: "you told me it was clean" | 1 | scripts/reap-worktrees.sh + session-relay-{resume,wrap-up} | applied |
| 2026-06-18 | forge-loop | TICK-ON-MERGED, not tick-on-armed: "guardian PASS + `gh pr merge --auto`" only ARMS a merge (a required check can still block it). Terminal Done must require `gh pr view <N>` == MERGED; armed-not-merged stays in-flight (`- [~]`) and is re-polled. | #125 ticked TASK-0027 before its PR merged → had to be retired as superseded | 1 | skills/forge-loop (steps 4-5) + session-relay-wrap-up (3b) | applied |
| 2026-06-18 | feature-forge | A retirement/absorption is not DONE until the on-disk artifacts the new code RESOLVES are migrated AND any claimed enforcement actually runs — a documented-but-unwired gate is worse than none (false assurance). Verify "CI enforces X" against the actual workflow. | TASK-0018 retired kasetto but left `kasetto.yaml/.lock` unmigrated → absorbed CLI resolved `agent-env.yaml` (config not found) → the CLAUDE.md-claimed `--locked` gate never existed and couldn't have worked (TASK-0040) | 1 | ci/gates/agent-env.sh + agent-env.{yaml,lock} migration + CLAUDE.md | applied |
| 2026-06-18 | forge-loop | High-churn shared state committed in feature branches needs a merge DRIVER, not git's default 3-way — append-heavy files (loop_state/backlog) silently CONCATENATE across non-overlapping regions and report a clean merge. A driver that forces a visible conflict makes silent corruption impossible. | cycle 5 (2026-06-13): 3-way merge of #47/#48/#49 triplicated the loop_state header + duplicated TASK cards, undetected | 1 | .gitattributes + scripts/handoff-merge-guard.sh (+install at resume) | applied |
| 2026-06-18 | feature-forge | Prevent drift at PICK-time, not just at wrap-up: a frozen-consumer-contract check that runs only as a reactive end-of-session sweep fires a cycle too late. Grep for a frozen CLI/RPC/JSON contract BEFORE designing the item. | G2/TASK-0020 built `secretctl relay mint --mode native` while the frozen contract was `secretctl mint-github → {token,expires_at_unix}` — App stayed 404, whole cycle missed the target; only the wrap-up sweep caught it | 2 (1st = 2026-06-17 wrap-up 3b — reactive form, SAME G2/TASK-0020 evidence, CLAUDE.md row 188; never rowed in the ledger then. Textbook once→noted / 2nd→upgrade-now escalation that correctly fired: reactive wrap-up sweep → proactive pick-time check) | skills/feature-forge (Phase 0 step 5) | applied |
| 2026-06-18 | forge-loop | When a self-cleaning mechanism exists for ONE consistency surface, check every PARALLEL surface — a fix on the remote leaves the local mirror stale. Sync all of them, FF-only + clean-only (never over uncommitted work). | origin auto-deletes merged heads + the sync-master workflow FFs origin/master, but the LOCAL main-checkout `master` didn't auto-FF (caught only by `/verify`) | 1 | scripts/reap-worktrees.sh (step 1b FF-sync) | applied |
| 2026-06-18 | forge-loop | Destructive/irreversible automation must mirror the product's own invariants: dry-run-by-default, never `--force`, protect-and-skip (skip dirty, protect master/develop/current). This is also why the runtime's safety classifier correctly blocked the manual variants — the automation encodes the safety the ad-hoc command lacked. | reaper + remote-prune designed fail-closed; classifier blocked the ad-hoc `checkout --`/remote-delete/`sync --locked` until verified, validating the design | 1 | scripts/reap-worktrees.sh (design) | applied |
| 2026-06-18 | forge-loop | The squash-robust merge ORACLE is the GitHub merged-PR head-ref, not the local `[gone]`/ancestor heuristics. After a squash merge the tip is never an ancestor of master, and a deleted upstream reads `[gone]` whether the PR was **merged** OR **closed-unmerged** — so `[gone]` alone can destroy unmerged work. Before any irreversible delete, confirm the branch's PR `state == MERGED` (`gh pr list --state merged`/`gh pr view`); the local heuristics are a safe *reap-after-confirmed* filter, never the merge proof. | this session: 14 remotes verified against GitHub merged-PR history before delete; `#99` was `[gone]` but **closed-not-merged** — heuristic would have mis-classified it as merged | 1 | scripts/reap-worktrees.sh (CAVEAT comment: `[gone]` ≠ merged; use `gh pr` oracle for irreversible remote deletes) | applied |
| 2026-06-18 | forge-loop | Irreversible EXTERNAL mutations (remote-branch deletes, security-advisory dismissals, anything that leaves the repo and can't be `git reset`) are a correct human wall — gate them on explicit owner authorization AND leave a written, reversible-as-possible trail (recovery manifest of deleted refs before deletion; advisory-disposition rationale block in-tree). This is distinct from in-repo dry-run gating: dry-run protects local state, owner-authz + manifest protects against irreversible off-box actions. | 14 remote deletes + GHSA-8m95-fffc-h4c5 Dependabot dismissal were owner-authorized; a `envctl-branch-recovery-2026-06-18.txt` manifest was written before deletes; the dismissal rationale was recorded in `crates/secrets-store-libsql/Cargo.toml` | 1 | session-relay-wrap-up (step 5b "local reap vs remote delete" discipline note) | applied |
| 2026-06-23 | forge-loop | Write-side complement of TICK-ON-MERGED: once `gh pr merge --auto` is armed, treat the PR branch as potentially-already-merged. A fast PR can merge (and origin can auto-delete the head, `delete_branch_on_merge=true`) within seconds — a follow-up push then RE-CREATES the branch as a dangling no-PR ref and the fix-up never reaches the merged code. Never push a post-arm fix-up without first confirming `gh pr view <N> --json state` is still `OPEN`; otherwise apply it BEFORE arming, or via a fresh reconcile branch off `develop` after `MERGED`. (Adjacent to TICK-ON-MERGED, but about the WRITE side, not the read/tick side.) | TASK-0060/PR #172: post-arm `pr=<PR>`→`pr=172` backlog fix pushed without re-poll; #172 had already MERGED + head auto-deleted → push re-created `task-0060-ollama` as a dangling orphan branch; caught (`[new branch]` + `gh pr view`=MERGED), deleted, re-driven via reconcile branch off develop (PR #173) | 1 | skills/forge-loop (TICK-ON-MERGED block — one-line write-side note) | applied |
| 2026-06-22 | feature-forge | RECURRENCE-CONFIRM (no new upgrade): the verify-against-HEAD-before-designing class (rows 2026-06-17 Phase-0 step 4 + 2026-06-18 Phase-0 step 5) generalizes beyond a *suspect stale relay claim* to a *fresh acceptance-criteria fork* — the architect ran a per-AC "vs HEAD" check unprompted, found AC2-AC5 already satisfied by the frozen `mint-github --permissions` path (TASK-0020/0026/0028), and resolved the build-new-POLICY_DRIFT-surface-vs-document-existing fork to **document+test existing** → avoided a redundant code surface. The encoded Phase-0 checks worked as designed; recording recurrence, not appending a new lesson. | TASK-0053 cycle: architect plan `## Triggering-claim check (per acceptance criterion vs HEAD)` resolved the U-fork to docs+1 additive test, GREEN/PASS 0 blocking (01_architect_plan.md; 03_guardian_report.md) | 3 (Phase-0 verify-vs-HEAD class; 1st=2026-06-17 stale-claim, 2nd=2026-06-18 frozen-contract pick-time, 3rd=this AC-fork) | skills/feature-forge (Phase 0) — already applied; no new edit | noted |
| 2026-06-23 | forge-loop | Don't dodge an infra limit — AUTHENTICATE with the infra the owner already built. When automation hits a quota/permission wall (GitHub API 403), the fix is to use the available credential, not a workaround. The box is `gh`-authed (5000/hr vs the 60/hr unauth that was exhausted), and `gh` is itself a meta-owned component → Epic-H GitHub fetches should use authenticated `gh release download`/`gh api`. Also: never sit and idle-poll a background job — research/use infra in parallel; the harness re-invokes on completion. | owner: "never ever sit and poll … why not research the rate limit … leverage the full-permission github app." nix-portable (#179) + llvm (#180) converted to authenticated gh; the vault App path filed (TASK-0069) as the more-isolated future source (blocked: secretctl off-PATH + App enroll 404) | 1 | manifest gh-auth fetch (epic-h-toolchains.toml) + ICM 01KVT101NCCZ7PWD7B3FXSHYXN | applied |
| 2026-06-23 | forge-loop | A cycle branch cut from a develop that is BEHIND an in-flight wrap-up PR must not allocate NEW backlog card numbers (or edit backlog.md) — it can't see the wrap-up's just-filed cards and will collide. Keep the cycle PR to its code/manifest surface; defer ALL backlog edits (the item tick AND any new follow-up card) to the wrap-up, which runs off fresh post-merge develop where the latest numbers are visible. | TASK-0054 implementer (off pre-#181 develop) numbered the mold-drop card "TASK-0067", colliding with #181's in-flight SUPERVISED /nix-migration TASK-0067; orchestrator reverted the stale backlog edit from the cycle PR and filed the card as TASK-0070 in the wrap-up | 1 | session-relay-wrap-up (backlog edits are wrap-up-only, off fresh develop) | applied |
| 2026-06-23 | feature-forge | Vendor self-extracting installers (makeself: CUDA `.run`, many `.sh` bundles) try to spawn a terminal/xterm when stdout is NOT a TTY — under automation they die `exec: -title` (exit 127) with `$XTERM` empty. A component install hook that shells one MUST pass the headless flag (`--nox11` for CUDA/makeself) AND `</dev/null`. This class of bug is invisible to manifest review + compiles-green and is caught ONLY by actually RUNNING the install (same as the kache migration-awk bug the same session) — reinforces the runtime-verify gate over static review for install hooks. | TASK-0063/#186: `sh cuda_13.3.0…run --silent --toolkit …` failed `457: exec: -title` headless; adding `--nox11` + `</dev/null` made the real 7.0G install succeed (nvcc 13.3 from the meta prefix) | 1 | manifest/gpu.toml cuda-toolkit install/fix + LESSONS (install-hook runtime-verify) | applied |
| 2026-06-23 | feature-forge | Build-affecting wiring (RUSTC_WRAPPER, cargo linker) belongs in the meta-root `.cargo/config.toml`, NOT the `envctl env` env-seam: cargo reads its config in EVERY context (interactive, login, non-interactive scripts, CI), whereas the env-seam (`eval "$(envctl env)"` in `~/.bashrc`) is sourced only by INTERACTIVE shells — `bash -lc` (login) and bare build scripts/hooks skip `.bashrc`, so an env-var wiring is silently inert there. (Nuance correction: the seam IS live interactively; the earlier "not sourced interactively" read was imprecise — the real gap is login/non-interactive contexts.) | TASK-0055/#184: kache wired via `[build] rustc-wrapper` in the co-managed `.cargo/config.toml` (works for all builds), not RUSTC_WRAPPER env; confirmed `LIBCLANG_PATH`/`GCC_PATH` empty under `bash -lc` but the `.bashrc` `eval envctl env` runs interactively | 1 | skills/rust-feature-impl (wiring: prefer cargo config over env-seam for build-affecting settings) | applied |
| 2026-06-23 | feature-forge | When two components must write the SAME runtime config file, each owns a delimited `# >>> name >>> … # <<< name <<<` block written by a NON-CLOBBERING upsert (strip-my-block, append-fresh) — never wholesale `cat >` (which silently destroys the sibling's section). Migration off a prior wholesale format must stop its strip at the next `[section]` OR comment marker, or it over-consumes the following block's begin-marker (then a later upsert can't find it → duplicate section → invalid TOML). | TASK-0055/#184: `.cargo/config.toml` co-managed by wild (`[target]`) + kache (`[build]`); legacy-strip awk first ate the kache begin-marker — caught by RUNNING the install, fixed to stop at `^#` too | 1 | manifest/components.d/epic-h-toolchains.toml (wild+kache block-upsert) | applied |
| 2026-06-23 | forge-loop | When a subagent dies mid-cycle (weekly model limit / terminal API error), the orchestrator must finish the cycle deterministically rather than abandon it — the agent's edits persist in the worktree, so the orchestrator runs the mandatory gate (here: the build-verification gate) + the guardian checks itself and commits. A near-complete cycle left uncommitted is lost work; the durable worktree state is the recovery point. | TASK-0054: implementer hit the weekly limit after writing the wild-linker component edit but before commit/gate; orchestrator ran the build-verify gate (cargo clean+build -v → `--ld-path=wild`, GREEN), wrote the guardian report, and committed (#182) | 1 | forge-loop (orchestrator completes a limit-interrupted cycle from worktree state) | applied |
| 2026-06-27 | plan-loop | Graph-before-analysis ordering: the code graph is an INPUT to per-dimension analysis, so the axis auditors/analyst must gate on the cartographer's graph JSON being materialized first (topological dependency), OR be told explicitly the graph may lag and to declare the gap rather than proceed graph-blind. Declaring the gap (not fabricating a query) is the correct fail-closed behavior; the fix is to remove the race so depth isn't lost. | Cycle 5 (grit): analyst/axis auditors ran before cartographer graph JSON existed; analyst noted the absence honestly (`findings/architecture-grit.md:10` "noted gap, not a fabricated graph query") but lost graph-derived-metric depth | 1 | PROPOSE: plan-loop orchestrator (ready-set scheduling — graph node is an upstream dep of analysis nodes) | proposed |
| 2026-06-27 | plan-loop | Phantom-workspace / sibling-escape environment hazard: a stray `Cargo.toml` in an ancestor dir makes Cargo walk UP and silently absorb the target into a foreign virtual workspace, breaking standalone `cargo build`/CI. A loop that runs in worktrees must scan for and neutralize stray ancestor manifests as a PRE-CYCLE environment check (or pin the target with an empty `[workspace]`), before any build/verify gate runs. | Cycle 5 (grit): stray `meta/.worktrees/Cargo.toml` blocked grit standalone build; verifier confirmed the remediation feasible (verdicts.md FEASIBILITY phantom-workspace row) | 2 (1st = handoff A-U1 sibling-escape class; 2nd = this cycle) | PROPOSE + FLAG-APPLY: pre-cycle env check for stray ancestor manifests (proposed-upgrades.md) | proposed |
| 2026-06-27 | plan-loop | Self-referential sentinel-token gate trap: an auditor that quotes the completeness-gate's own rejection vocabulary verbatim (the placeholder/uncertainty sentinel words the gate rejects case-insensitively) trips the gate ON ITS OWN findings. Auditors must reference that class obliquely (describe the sentinel category, never write the literal tokens) so an honest finding about gaps still passes the gate. STRENGTHENS reliability — does not weaken the gate's strictness. | Cycle 5 (grit): multiple auditors had to be warned before their findings tripped the gate; recurrence of the same class first seen cycle 4 | 2 (1st = cycle 4; 2nd = this cycle) | FLAG-APPLY: plan-* auditor skill bodies (oblique-reference rule + example) — queued for batch boundary | proposed |
| 2026-06-27 | plan-loop | Weave as in-cycle cross-loop coordination (NEW capability): planning loops can use a weave A2A round-trip DURING a cycle to get another loop/session to verify a plan, fold the corrections, and ship — not only for async handoff heartbeats. The transport plane (cycle-4 finding) is now proven end-to-end loop-to-loop. Harness should document this as an available coordination primitive (still propose-by-default; weave is observable, the committed artifact remains authoritative). | Cycle 5: envctl asked rusty-idd over weave to verify the lifeos-meta-front-door plan; rusty-idd returned 3 corrections (D3 two-layer front door, D1 union binding, grit-unfit caveat); envctl folded them and shipped prompt_hub PR #182 | 1 | PROPOSE: harness note — weave is an in-cycle coordination primitive (proposed-upgrades.md) | proposed |
| 2026-06-28 | forge-loop | Owner-supervised migrations should climb a review ladder in separate PRs — status/precondition, validation, scaffold, explicit writer, and only then any live movement. Do not combine manifest creation and state migration in one command or cycle; the apply surface must make review observable between steps. | TASK-0078 boundary 54: PR #368 missing-manifest refusal, #369 id validation, #370 validation TSV, #371 scaffold TSV, #373 writer ordered after migrate attempts so one invocation cannot create and move `.cache/.wasm-pack` | 1 | TASK-0078/meta-local migration practice + feature-forge runtime review discipline | noted |
