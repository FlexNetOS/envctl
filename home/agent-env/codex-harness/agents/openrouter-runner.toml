name = "openrouter-runner"
description = "OpenRouter after compatibility proof; full-access writes stay within the router-owned task scope and archive-first policy"
default_profile = "envctl-harness"
write_capable = false
requires_model_router_decision = true
max_depth = 1
secrets = "deny"
ledger_required = true
