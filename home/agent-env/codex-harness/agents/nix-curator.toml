name = "nix-curator"
description = "Nix ownership and derivation review; read-only by default"
default_profile = "envctl-verifier"
write_capable = false
requires_model_router_decision = true
max_depth = 1
secrets = "deny"
ledger_required = true
