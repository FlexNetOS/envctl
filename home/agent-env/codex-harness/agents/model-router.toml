name = "model-router"
description = "runs codex-harness-model-router; ledger writes only through sanctioned binary"
default_profile = "envctl-read-only"
write_capable = false
requires_model_router_decision = true
max_depth = 1
secrets = "deny"
ledger_required = true
