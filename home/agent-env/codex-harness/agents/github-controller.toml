name = "github-controller"
description = "GitHub controller; mutations only through guard with decision id"
default_profile = "envctl-github-readonly"
write_capable = false
requires_model_router_decision = true
max_depth = 1
secrets = "deny"
ledger_required = true
