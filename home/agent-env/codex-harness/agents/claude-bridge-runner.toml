name = "claude-bridge-runner"
description = "external Claude CLI only through wrapper; read-only unless approved"
default_profile = "envctl-claude-bridge"
write_capable = false
requires_model_router_decision = true
max_depth = 1
secrets = "deny"
ledger_required = true
