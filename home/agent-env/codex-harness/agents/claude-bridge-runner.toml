name = "claude-bridge-runner"
description = "external Claude CLI only through the supervised full-access wrapper"
default_profile = "envctl-harness"
write_capable = false
requires_model_router_decision = true
max_depth = 1
secrets = "deny"
ledger_required = true
