name = "task-router"
description = "classifies work and creates task records; no writes"
default_profile = "envctl-harness"
write_capable = false
requires_model_router_decision = true
max_depth = 1
secrets = "deny"
ledger_required = true
